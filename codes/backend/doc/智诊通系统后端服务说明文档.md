# 智诊通系统后端服务说明文档

## 📋 系统概述

智诊通后端基于 FastAPI 构建，提供用户认证、对话管理、诊断、知识检索、多模态处理等 API。采用模块化设计，内置 CORS、可信主机、全局异常、健康检查、自定义 Swagger、开放 API 安全模型等能力，并集成 Redis、PostgreSQL 与向量数据库等外部组件。

### 🎯 核心特性

- 高性能异步 API（FastAPI + Uvicorn）
- 生命周期管理（启动检查 DB/Redis，建表、健康日志）
- 全局中间件与异常处理、请求耗时统计
- 自定义 OpenAPI/Swagger UI 与 JWT 安全模型
- 模块化路由：认证、对话、诊断、知识、多模态、系统
- 健康检查 `/health` 与调试 `/debug/info`（DEBUG 模式）

---

## 🏗️ 技术栈与依赖

- 语言与框架：Python 3.9+、FastAPI、Uvicorn
- 配置：pydantic-settings
- 数据库：PostgreSQL（SQLAlchemy/ORM，见 `database.py`、`models/`）
- 缓存：Redis（`cache.py`）
- 向量/检索：与上游 检索/Embedding 服务对接
- 日志：标准日志与文件日志（`./logs/backend.log`）

依赖见：`codes/backend/requirements.txt`

---

## 📁 目录结构

```
codes/backend/
├── app/                           # 主应用目录
│   ├── api/                       # API路由模块
│   │   ├── auth.py                # 认证相关端点
│   │   ├── conversations.py       # 对话与消息
│   │   ├── diagnosis.py           # 诊断接口（模拟/占位）
│   │   ├── knowledge.py           # 知识检索（占位/代理）
│   │   ├── multimodal.py          # 多模态相关
│   │   ├── patients.py            # 患者管理
│   │   ├── files.py               # 文件管理
│   │   ├── system.py              # 系统信息与健康
│   │   └── v1/                    # API v1版本路由
│   ├── config/                    # 配置管理
│   │   └── settings.py            # 系统配置
│   ├── core/                      # 核心功能
│   │   └── streaming_config.py    # 流式配置
│   ├── models/                    # 数据模型
│   │   ├── base.py                # 基础模型
│   │   ├── user.py                # 用户模型
│   │   ├── conversation.py        # 对话模型
│   │   ├── diagnosis.py           # 诊断模型
│   │   ├── knowledge.py           # 知识模型
│   │   ├── multimodal.py          # 多模态模型
│   │   ├── patient.py             # 患者模型
│   │   ├── preference.py          # 偏好模型
│   │   ├── config.py              # 配置模型
│   │   └── log.py                 # 日志模型
│   ├── modules/                   # 业务模块
│   │   ├── conversation/          # 对话管理
│   │   ├── diagnosis/             # 诊断引擎
│   │   ├── multimodal/            # 多模态处理
│   │   └── retrieval/             # 检索服务
│   ├── services/                  # 领域服务
│   │   ├── diagnosis_service.py   # 诊断服务
│   │   └── retrieval_service.py   # 检索服务
│   ├── utils/                     # 工具函数
│   │   └── db_logger.py           # 数据库日志
│   ├── auth.py                    # 认证工具
│   ├── cache.py                   # Redis缓存
│   ├── database.py                # 数据库连接
│   ├── main.py                    # 应用入口
│   └── README.md                  # 应用说明
├── logs/                          # 日志目录
├── uploads/                       # 上传文件目录
├── vector_db/                     # 向量数据库
├── scripts/                       # 脚本目录
├── docker-compose.dev.yml         # Docker开发配置
├── env.example                    # 环境变量示例
├── requirements.txt               # Python依赖
├── start-dev.sh                   # 开发启动脚本
├── start-database.sh              # 数据库启动脚本
└── 后端系统说明文档.md            # 原系统说明文档
```

---

## ⚙️ 配置与环境

### 配置文件：`app/config/settings.py`

主要配置项：

- **服务器配置**：`SERVER_HOST`、`SERVER_PORT`（默认 0.0.0.0:8000）
- **数据库配置**：`DATABASE_URL`（默认 Postgres）
- **Redis 配置**：`REDIS_URL`、`REDIS_PASSWORD`
- **JWT 配置**：`SECRET_KEY`、`ALGORITHM`、`ACCESS_TOKEN_EXPIRE_MINUTES`
- **CORS 配置**：`CORS_ORIGINS`（开发含 8080）
- **上传配置**：`UPLOAD_DIR`、`MAX_FILE_SIZE`、`ALLOWED_FILE_TYPES`
- **AI 模型配置**：`OPENAI_API_KEY`、`OPENAI_BASE_URL`、`OPENAI_MODEL`
- **服务配置**：`RETRIEVAL_SERVICE_URL`、`DIAGNOSIS_SERVICE_URL`

环境变量示例：`codes/backend/env.example`

---

## 🚀 启动与运行

### 本地运行

```bash
cd codes/backend
pip install -r requirements.txt
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

启动日志将检查数据库与 Redis 连接并自动建表。

### Docker 运行

```bash
cd codes/backend
docker compose up -d --build
```

---

## 🔐 安全与文档

- 自定义 OpenAPI 安全方案：`BearerAuth`（JWT）
- 文档地址：`/docs`（自定义 Swagger UI），`/openapi.json`
- 所有受保护端点需 `Authorization: Bearer <token>`

---

## 🔌 API 概览

### 系统端点

- `GET /health` - 健康检查（状态、版本、外部服务）
- `GET /` - 根信息（功能列表、文档链接）
- `GET /debug/info` - 调试信息（仅 DEBUG）

### 认证端点（`/auth`）

- `POST /auth/register` - 用户注册
- `POST /auth/login` - 用户登录（返回 `access_token` 与 `user`）
- `POST /auth/logout` - 用户登出
- `GET /auth/me` - 获取当前用户信息（需 JWT）
- `PUT /auth/me` - 更新当前用户档案（需 JWT）

### 对话端点（`/api/v1`）

- `GET /api/v1/conversations/` - 获取对话列表
- `GET /api/v1/conversations/{id}` - 获取对话详情
- `POST /api/v1/conversations/` - 创建新对话
- `DELETE /api/v1/conversations/{id}` - 删除对话
- `POST /api/v1/conversations/{id}/messages` - 发送消息（同步）
- `POST /api/v1/conversations/{id}/messages/stream` - 流式消息（SSE）

### 其他端点

- `app/api/diagnosis.py` - 诊断相关接口（模拟数据）
- `app/api/knowledge.py` - 知识检索接口（代理服务）
- `app/api/multimodal.py` - 多模态处理接口
- `app/api/patients.py` - 患者管理接口
- `app/api/files.py` - 文件管理接口

---

## 🔄 中间件与异常

- **CORS**：允许跨域（开发环境为 `*`）
- **TrustedHost**：限制来源（`ALLOWED_HOSTS`）
- **全局 HTTP 中间件**：记录请求、统计耗时、附加 `X-Process-Time`
- **全局异常处理**：`Exception`、`HTTPException` 统一 JSON 输出

---

## 🧪 验证与联调

### 健康检查

```bash
curl http://localhost:8000/health
```

### Swagger 文档

```bash
open http://localhost:8000/docs
```

### 前端联调（Vite 代理）

- `vite.config.ts` 将 `/api`、`/auth`、`/health`、`/ws` 代理至后端
- 前端认证与会话管理对齐 `/auth/*` 与 `/api/v1/*` 路由

---

## ⚠️ 故障排除

| 错误代码 | 描述           | 可能原因               | 解决方案                        |
| -------- | -------------- | ---------------------- | ------------------------------- |
| BE001    | 数据库连接失败 | URL/网络/权限错误      | 校验 `DATABASE_URL`，检测网络   |
| BE002    | Redis 连接失败 | 地址/密码错误、未启动  | 校验 `REDIS_URL`，启动服务      |
| BE003    | 未携带令牌     | 前端未传 Authorization | 在请求头添加 `Bearer <token>`   |
| BE004    | CORS 预检失败  | 跨域未配置正确         | 配置 `CORS_ORIGINS` 与允许方法  |
| BE005    | SSE 无数据     | 响应头/换行不规范      | 返回 `text/event-stream` 与换行 |

---

## 📈 最佳实践

- 严格区分路由层、服务层与数据层职责
- 统一错误返回结构与日志格式，便于观测
- 所有外部依赖（DB/Redis）启动前健康检查
- 开发与生产分离配置（DEBUG/CORS/日志级别）

---

## 📄 版本与状态

- **版本**：v1.0.0
- **更新时间**：2025-01
- **状态**：🟢 可用（前后端联调通过，SSE/认证已验证）

---

## 📝 开发说明

### 当前开发状态

- ✅ 后端 API 框架已完成
- ✅ 数据库设计和 ORM 模型
- ✅ 用户认证和权限管理
- ✅ 多模态处理接口（模拟数据）
- ✅ 智能诊断接口（模拟数据）
- ✅ 知识库检索接口（模拟数据）
- ✅ 对话管理系统
- 🔄 算法服务开发中（当前使用模拟数据）

### 使用说明

1. 首先通过 `/auth/register` 注册用户
2. 使用 `/auth/login` 登录获取访问令牌
3. 在请求头中添加 `Authorization: Bearer <token>` 进行认证
4. 开始使用各种功能接口

### 注意事项

- 当前算法相关接口返回模拟数据，用于前端开发测试
- 所有敏感操作都需要有效的 JWT 令牌
- 文件上传支持多种格式，大小限制为 10MB

---

_本文档基于现有系统分析生成，详细 API 文档请参考 `/docs` 接口_
