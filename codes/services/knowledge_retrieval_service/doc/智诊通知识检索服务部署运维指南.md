# 智诊通知识检索服务部署运维指南

## 📋 部署运维概述

智诊通知识检索服务部署运维指南提供了完整的服务部署、配置、监控、维护和故障排除方案。该指南涵盖了从开发环境到生产环境的完整部署流程，确保服务的稳定运行和高效维护。

### 🎯 核心内容

- **环境准备**：系统要求、依赖安装、环境配置
- **部署方案**：多种部署方式、配置管理、服务启动
- **监控运维**：性能监控、日志管理、故障排除
- **安全配置**：访问控制、数据加密、安全加固
- **备份恢复**：数据备份、灾难恢复、版本管理
- **性能优化**：性能调优、资源优化、扩展方案

---

## 🏗️ 系统架构

### 部署架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    智诊通知识检索服务部署架构                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────┐    ┌─────────────────┐                │
│  │   负载均衡器     │    │   反向代理       │                │
│  │   (Nginx)       │    │   (Nginx)       │                │
│  └─────────────────┘    └─────────────────┘                │
│           │                       │                        │
│           └───────────┬───────────┘                        │
│                       │                                    │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                服务实例集群                              │ │
│  │                                                         │ │
│  │ • 知识检索服务实例 (8002)                               │ │
│  │ • 向量化服务实例 (8001)                                 │ │
│  │ • ES检索服务实例 (8004)                                 │ │
│  │ • 混合检索服务实例 (8005)                               │ │
│  │ • 智能诊断服务实例 (8003)                               │ │
│  └─────────────────────────────────────────────────────────┘ │
│                       │                                    │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                数据存储层                                │ │
│  │                                                         │ │
│  │ • ChromaDB (向量数据库)                                 │ │
│  │ • ElasticSearch (文档索引)                              │ │
│  │ • Redis (缓存)                                         │ │
│  │ • 文件存储 (文档存储)                                   │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 服务依赖关系

```
知识检索服务 (8002)
├── 依赖 向量化服务 (8001)
├── 依赖 ES检索服务 (8004)
├── 依赖 混合检索服务 (8005)
├── 使用 ChromaDB (向量数据库)
├── 使用 ElasticSearch (文档索引)
└── 使用 Redis (缓存)
```

---

## 🔧 环境准备

### 1. 系统要求

#### 最低配置要求

**硬件要求**：

- **CPU**: 4 核心 2.0GHz
- **内存**: 8GB RAM
- **存储**: 50GB 可用空间
- **网络**: 100Mbps 带宽

**推荐配置**：

- **CPU**: 8 核心 3.0GHz
- **内存**: 16GB RAM
- **存储**: 100GB SSD
- **网络**: 1Gbps 带宽

**生产环境配置**：

- **CPU**: 16 核心 3.5GHz
- **内存**: 32GB RAM
- **存储**: 500GB SSD
- **网络**: 10Gbps 带宽

#### 软件要求

**操作系统**：

- Ubuntu 20.04 LTS 或更高版本
- CentOS 8 或更高版本
- Windows 10/11 (开发环境)

**Python 环境**：

- Python 3.8+
- pip 20.0+
- conda (推荐)

**依赖软件**：

- CUDA 11.0+ (GPU 环境)
- cuDNN 8.0+ (GPU 环境)
- Docker 20.0+ (容器部署)
- Nginx 1.18+ (反向代理)

### 2. 依赖安装

#### 2.1 Python 依赖

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate  # Windows

# 安装基础依赖
pip install -r requirements.txt

# 安装GPU支持 (可选)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

#### 2.2 系统依赖

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install -y python3-pip python3-venv git curl wget
sudo apt install -y nginx redis-server

# CentOS/RHEL
sudo yum update
sudo yum install -y python3-pip python3-venv git curl wget
sudo yum install -y nginx redis

# 安装Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```

#### 2.3 GPU 环境配置

```bash
# 安装NVIDIA驱动
sudo apt install nvidia-driver-470

# 安装CUDA
wget https://developer.download.nvidia.com/compute/cuda/repos/ubuntu2004/x86_64/cuda-ubuntu2004.pin
sudo mv cuda-ubuntu2004.pin /etc/apt/preferences.d/cuda-repository-pin-600
wget https://developer.download.nvidia.com/compute/cuda/11.8.0/local_installers/cuda-repo-ubuntu2004-11-8-local_11.8.0-520.61.05-1_amd64.deb
sudo dpkg -i cuda-repo-ubuntu2004-11-8-local_11.8.0-520.61.05-1_amd64.deb
sudo cp /var/cuda-repo-ubuntu2004-11-8-local/cuda-*-keyring.gpg /usr/share/keyrings/
sudo apt-get update
sudo apt-get -y install cuda

# 验证安装
nvidia-smi
nvcc --version
```

---

## 🚀 部署方案

### 1. 开发环境部署

#### 1.1 快速部署

```bash
# 克隆代码
git clone <repository-url>
cd knowledge_retrieval_service

# 安装依赖
pip install -r requirements.txt

# 配置环境变量
export PYTHONPATH=$PWD:$PYTHONPATH

# 启动服务
python start_retrieval_service.py
```

#### 1.2 详细部署步骤

```bash
# 1. 环境准备
python3 -m venv venv
source venv/bin/activate

# 2. 安装依赖
pip install --upgrade pip
pip install -r requirements.txt

# 3. 配置服务
cp api/config/retrieval_config.json.example api/config/retrieval_config.json
# 编辑配置文件
vim api/config/retrieval_config.json

# 4. 启动服务
python start_retrieval_service.py --host 0.0.0.0 --port 8002

# 5. 验证部署
curl http://localhost:8002/health
```

### 2. Docker 部署

#### 2.1 Dockerfile

```dockerfile
FROM python:3.9-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    git \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 创建非root用户
RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser

# 暴露端口
EXPOSE 8002

# 健康检查
HEALTHCHECK --interval=30s --timeout=10s --start-period=60s --retries=3 \
    CMD curl -f http://localhost:8002/health || exit 1

# 启动命令
CMD ["python", "start_retrieval_service.py"]
```

#### 2.2 Docker Compose

```yaml
version: "3.8"

services:
  # 知识检索服务
  knowledge-retrieval:
    build: .
    container_name: knowledge-retrieval-service
    ports:
      - "8002:8002"
    environment:
      - PYTHONPATH=/app
      - CONFIG_PATH=/app/api/config/retrieval_config.json
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./api/config:/app/api/config
    depends_on:
      - redis
      - chromadb
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          memory: 4G
          cpus: "2.0"
        reservations:
          memory: 2G
          cpus: "1.0"

  # Redis缓存
  redis:
    image: redis:7-alpine
    container_name: knowledge-retrieval-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    command: redis-server --appendonly yes

  # ChromaDB
  chromadb:
    image: chromadb/chroma:latest
    container_name: knowledge-retrieval-chromadb
    ports:
      - "8000:8000"
    volumes:
      - chromadb_data:/chroma/chroma
    restart: unless-stopped

  # Nginx反向代理
  nginx:
    image: nginx:alpine
    container_name: knowledge-retrieval-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - knowledge-retrieval
    restart: unless-stopped

volumes:
  redis_data:
  chromadb_data:
```

#### 2.3 部署命令

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f knowledge-retrieval

# 停止服务
docker-compose down

# 清理数据
docker-compose down -v
```

### 3. Kubernetes 部署

#### 3.1 部署配置

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-retrieval-service
  labels:
    app: knowledge-retrieval-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: knowledge-retrieval-service
  template:
    metadata:
      labels:
        app: knowledge-retrieval-service
    spec:
      containers:
        - name: knowledge-retrieval-service
          image: knowledge-retrieval-service:latest
          ports:
            - containerPort: 8002
          env:
            - name: PYTHONPATH
              value: "/app"
            - name: CONFIG_PATH
              value: "/app/api/config/retrieval_config.json"
          resources:
            requests:
              memory: "2Gi"
              cpu: "1"
            limits:
              memory: "4Gi"
              cpu: "2"
          volumeMounts:
            - name: config-volume
              mountPath: /app/api/config
            - name: data-volume
              mountPath: /app/data
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: config-volume
          configMap:
            name: knowledge-retrieval-config
        - name: data-volume
          persistentVolumeClaim:
            claimName: knowledge-retrieval-data
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-retrieval-service
spec:
  selector:
    app: knowledge-retrieval-service
  ports:
    - port: 8002
      targetPort: 8002
      protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: knowledge-retrieval-config
data:
  retrieval_config.json: |
    {
      "vector_service": {
        "device": "cpu",
        "vector_dim": 512,
        "batch_size": 32
      },
      "retrieval_service": {
        "max_results": 20,
        "similarity_threshold": 0.5,
        "retrieval_strategy": "hybrid"
      },
      "llm_service": {
        "device": "cpu",
        "model_path": "FreedomIntelligence/Apollo-0.5B",
        "max_length": 2048,
        "timeout": 600
      },
      "api": {
        "host": "0.0.0.0",
        "port": 8002,
        "log_level": "info"
      }
    }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: knowledge-retrieval-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
```

#### 3.2 部署命令

```bash
# 应用配置
kubectl apply -f k8s-deployment.yaml

# 查看部署状态
kubectl get pods -l app=knowledge-retrieval-service

# 查看服务状态
kubectl get svc knowledge-retrieval-service

# 查看日志
kubectl logs -f deployment/knowledge-retrieval-service

# 扩缩容
kubectl scale deployment knowledge-retrieval-service --replicas=5

# 删除部署
kubectl delete -f k8s-deployment.yaml
```

---

## 🔧 配置管理

### 1. 生产环境配置

#### 1.1 服务配置

```json
{
  "vector_service": {
    "device": "cuda",
    "vector_dim": 768,
    "batch_size": 64,
    "text_model_path": "text2vec-base-chinese",
    "image_model_path": "clip-vit-base-patch32",
    "use_knowledge_base_service": true
  },
  "retrieval_service": {
    "vector_dim": 768,
    "max_results": 20,
    "similarity_threshold": 0.7,
    "retrieval_strategy": "hybrid",
    "vector_db_path": "/data/chroma_db",
    "collection_name": "medical_knowledge",
    "model_name": "text2vec-base-chinese",
    "es_weight": 0.6,
    "vector_weight": 0.4,
    "rrf_k": 60,
    "max_es_results": 5,
    "max_vector_results": 5
  },
  "llm_service": {
    "device": "cuda",
    "model_path": "Medical_Qwen3_17B",
    "max_length": 2048,
    "temperature": 0.7,
    "top_p": 0.9,
    "top_k": 50,
    "repetition_penalty": 1.1,
    "timeout": 600
  },
  "summarization_service": {
    "device": "cuda",
    "model_path": "T5-small",
    "max_length": 100,
    "min_length": 30,
    "num_beams": 4,
    "temperature": 0.7,
    "early_stopping": true,
    "use_medical_summarization": true,
    "medical_max_length": 50,
    "timeout": 300
  },
  "api": {
    "host": "0.0.0.0",
    "port": 8002,
    "reload": false,
    "log_level": "warning"
  }
}
```

#### 1.2 环境变量配置

```bash
# 服务配置
export RETRIEVAL_SERVICE_HOST=0.0.0.0
export RETRIEVAL_SERVICE_PORT=8002
export RETRIEVAL_SERVICE_LOG_LEVEL=info

# 模型配置
export LLM_MODEL_PATH=/models/Medical_Qwen3_17B
export VECTOR_MODEL_PATH=/models/text2vec-base-chinese
export DEVICE=cuda

# 数据库配置
export VECTOR_DB_PATH=/data/chroma_db
export REDIS_URL=redis://localhost:6379
export ES_URL=http://localhost:9200

# 安全配置
export SECRET_KEY=your-secret-key
export JWT_SECRET=your-jwt-secret
```

### 2. Nginx 配置

#### 2.1 反向代理配置

```nginx
upstream knowledge_retrieval_backend {
    server 127.0.0.1:8002;
    server 127.0.0.1:8003;
    server 127.0.0.1:8004;
}

server {
    listen 80;
    server_name retrieval.example.com;

    # 重定向到HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name retrieval.example.com;

    # SSL配置
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # 安全头
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # 日志配置
    access_log /var/log/nginx/knowledge_retrieval_access.log;
    error_log /var/log/nginx/knowledge_retrieval_error.log;

    # 主要代理配置
    location / {
        proxy_pass http://knowledge_retrieval_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 缓冲设置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;
    }

    # 流式响应配置
    location /query/stream {
        proxy_pass http://knowledge_retrieval_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 流式响应设置
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # 健康检查
    location /health {
        proxy_pass http://knowledge_retrieval_backend;
        access_log off;
    }

    # 静态文件
    location /static/ {
        alias /var/www/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 2.2 负载均衡配置

```nginx
upstream knowledge_retrieval_cluster {
    least_conn;
    server 10.0.1.10:8002 weight=3 max_fails=3 fail_timeout=30s;
    server 10.0.1.11:8002 weight=3 max_fails=3 fail_timeout=30s;
    server 10.0.1.12:8002 weight=2 max_fails=3 fail_timeout=30s;
    server 10.0.1.13:8002 weight=2 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;
    server_name api.example.com;

    location / {
        proxy_pass http://knowledge_retrieval_cluster;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 健康检查
        proxy_next_upstream error timeout invalid_header http_500 http_502 http_503 http_504;
        proxy_next_upstream_tries 3;
        proxy_next_upstream_timeout 10s;
    }
}
```

---

## 📊 监控运维

### 1. 进程管理

#### 1.1 systemd 服务

```ini
[Unit]
Description=Knowledge Retrieval Service
After=network.target

[Service]
Type=simple
User=appuser
Group=appgroup
WorkingDirectory=/opt/knowledge_retrieval_service
Environment=PYTHONPATH=/opt/knowledge_retrieval_service
ExecStart=/opt/knowledge_retrieval_service/venv/bin/python start_retrieval_service.py
ExecReload=/bin/kill -HUP $MAINPID
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal
SyslogIdentifier=knowledge-retrieval

[Install]
WantedBy=multi-user.target
```

#### 1.2 supervisor 管理

```ini
[program:knowledge_retrieval_service]
command=/opt/knowledge_retrieval_service/venv/bin/python start_retrieval_service.py
directory=/opt/knowledge_retrieval_service
user=appuser
autostart=true
autorestart=true
redirect_stderr=true
stdout_logfile=/var/log/supervisor/knowledge_retrieval_service.log
stdout_logfile_maxbytes=50MB
stdout_logfile_backups=10
environment=PYTHONPATH="/opt/knowledge_retrieval_service"
```

### 2. 负载均衡

#### 2.1 Nginx 配置

```nginx
upstream knowledge_retrieval_backend {
    least_conn;
    server 127.0.0.1:8002 weight=3 max_fails=3 fail_timeout=30s;
    server 127.0.0.1:8003 weight=3 max_fails=3 fail_timeout=30s;
    server 127.0.0.1:8004 weight=2 max_fails=3 fail_timeout=30s;

    keepalive 32;
}

server {
    listen 80;
    server_name retrieval.example.com;

    location / {
        proxy_pass http://knowledge_retrieval_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # 缓冲设置
        proxy_buffering on;
        proxy_buffer_size 4k;
        proxy_buffers 8 4k;

        # 健康检查
        proxy_next_upstream error timeout invalid_header http_500 http_502 http_503 http_504;
        proxy_next_upstream_tries 3;
        proxy_next_upstream_timeout 10s;
    }

    # 流式响应配置
    location /query/stream {
        proxy_pass http://knowledge_retrieval_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # 流式响应设置
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
```

#### 2.2 HAProxy 配置

```haproxy
global
    daemon
    maxconn 4096
    log 127.0.0.1:514 local0

defaults
    mode http
    timeout connect 5000ms
    timeout client 50000ms
    timeout server 50000ms
    option httplog
    option dontlognull
    option redispatch
    retries 3

frontend knowledge_retrieval_frontend
    bind *:80
    default_backend knowledge_retrieval_backend

backend knowledge_retrieval_backend
    balance roundrobin
    option httpchk GET /health
    server web1 127.0.0.1:8002 check inter 2000ms rise 2 fall 3
    server web2 127.0.0.1:8003 check inter 2000ms rise 2 fall 3
    server web3 127.0.0.1:8004 check inter 2000ms rise 2 fall 3
```

### 3. 监控系统

#### 3.1 Prometheus 监控

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "knowledge_retrieval_rules.yml"

scrape_configs:
  - job_name: "knowledge-retrieval-service"
    static_configs:
      - targets: ["localhost:8002"]
    metrics_path: "/metrics"
    scrape_interval: 30s
    scrape_timeout: 10s
```

#### 1.2 Prometheus 监控

```yaml
# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "knowledge_retrieval_rules.yml"

scrape_configs:
  - job_name: "knowledge-retrieval-service"
    static_configs:
      - targets: ["localhost:8002"]
    metrics_path: "/metrics"
    scrape_interval: 30s
    scrape_timeout: 10s
```

#### 3.2 Grafana 仪表板

```json
{
  "dashboard": {
    "title": "Knowledge Retrieval Service",
    "panels": [
      {
        "title": "Request Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(http_requests_total[5m])",
            "legendFormat": "{{method}} {{endpoint}}"
          }
        ]
      },
      {
        "title": "Response Time",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))",
            "legendFormat": "95th percentile"
          }
        ]
      },
      {
        "title": "Error Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(http_requests_total{status=~\"5..\"}[5m])",
            "legendFormat": "5xx errors"
          }
        ]
      }
    ]
  }
}
```

### 4. 日志管理

#### 4.1 日志配置

```python
import logging
import logging.handlers
from pathlib import Path

def setup_logging(log_name: str, log_level: str = "INFO"):
    """设置日志配置"""

    # 创建日志目录
    log_dir = Path("logs")
    log_dir.mkdir(exist_ok=True)

    # 配置日志格式
    formatter = logging.Formatter(
        '%(asctime)s - %(name)s - %(levelname)s - %(funcName)s:%(lineno)d - %(message)s'
    )

    # 文件处理器
    file_handler = logging.handlers.RotatingFileHandler(
        log_dir / f"{log_name}.log",
        maxBytes=100*1024*1024,  # 100MB
        backupCount=10
    )
    file_handler.setFormatter(formatter)

    # 控制台处理器
    console_handler = logging.StreamHandler()
    console_handler.setFormatter(formatter)

    # 配置根日志器
    root_logger = logging.getLogger()
    root_logger.setLevel(getattr(logging, log_level.upper()))
    root_logger.addHandler(file_handler)
    root_logger.addHandler(console_handler)

    return root_logger
```

#### 4.2 日志轮转

```bash
# /etc/logrotate.d/knowledge-retrieval-service
/opt/knowledge_retrieval_service/logs/*.log {
    daily
    missingok
    rotate 30
    compress
    delaycompress
    notifempty
    create 644 appuser appgroup
    postrotate
        systemctl reload knowledge-retrieval-service
    endscript
}
```

### 5. 性能监控

#### 5.1 系统指标

```python
import psutil
import time
from datetime import datetime

class SystemMonitor:
    def __init__(self):
        """初始化系统监控器"""
        self.metrics = {}

    def collect_metrics(self):
        """收集系统指标"""
        # CPU使用率
        cpu_percent = psutil.cpu_percent(interval=1)

        # 内存使用率
        memory = psutil.virtual_memory()

        # 磁盘使用率
        disk = psutil.disk_usage('/')

        # 网络IO
        network = psutil.net_io_counters()

        # 进程信息
        process = psutil.Process()

        self.metrics = {
            'timestamp': datetime.now().isoformat(),
            'cpu_percent': cpu_percent,
            'memory_percent': memory.percent,
            'memory_used': memory.used,
            'memory_available': memory.available,
            'disk_percent': disk.percent,
            'disk_used': disk.used,
            'disk_free': disk.free,
            'network_bytes_sent': network.bytes_sent,
            'network_bytes_recv': network.bytes_recv,
            'process_cpu_percent': process.cpu_percent(),
            'process_memory_percent': process.memory_percent(),
            'process_memory_rss': process.memory_info().rss,
            'process_memory_vms': process.memory_info().vms
        }

        return self.metrics

    def get_metrics(self):
        """获取指标"""
        return self.metrics
```

#### 5.2 应用指标

```python
from prometheus_client import Counter, Histogram, Gauge, start_http_server

# 定义指标
REQUEST_COUNT = Counter('http_requests_total', 'Total HTTP requests', ['method', 'endpoint', 'status'])
REQUEST_DURATION = Histogram('http_request_duration_seconds', 'HTTP request duration', ['method', 'endpoint'])
ACTIVE_CONNECTIONS = Gauge('active_connections', 'Number of active connections')
QUEUE_SIZE = Gauge('queue_size', 'Size of request queue')

class MetricsCollector:
    def __init__(self, port=8000):
        """初始化指标收集器"""
        self.port = port
        start_http_server(port)

    def record_request(self, method: str, endpoint: str, status: int, duration: float):
        """记录请求指标"""
        REQUEST_COUNT.labels(method=method, endpoint=endpoint, status=status).inc()
        REQUEST_DURATION.labels(method=method, endpoint=endpoint).observe(duration)

    def update_active_connections(self, count: int):
        """更新活跃连接数"""
        ACTIVE_CONNECTIONS.set(count)

    def update_queue_size(self, size: int):
        """更新队列大小"""
        QUEUE_SIZE.set(size)
```

---

## ⚠️ 故障排除

### 1. 常见问题

#### 1.1 服务启动失败

**问题**: 服务无法启动
**排查步骤**:

```bash
# 检查端口占用
netstat -tlnp | grep 8002
lsof -i :8002

# 检查进程
ps aux | grep python
ps aux | grep retrieval

# 检查日志
tail -f logs/retrieval_service.log
journalctl -u knowledge-retrieval-service -f

# 检查依赖
pip list | grep -E "(torch|transformers|fastapi)"
```

**解决方案**:

- 修改端口配置
- 检查依赖安装
- 检查配置文件
- 检查权限设置

#### 1.2 模型加载失败

**问题**: 模型无法加载
**排查步骤**:

```bash
# 检查模型文件
ls -la models/
du -sh models/*

# 检查磁盘空间
df -h

# 检查内存使用
free -h
top -p $(pgrep -f python)

# 检查GPU状态
nvidia-smi
```

**解决方案**:

- 重新下载模型
- 检查 GPU 驱动
- 验证 CUDA 安装
- 调整设备配置

#### 1.3 内存使用过高

**问题**: 内存使用过高
**排查步骤**:

```bash
# 检查内存使用
free -h
top -p $(pgrep -f python)

# 检查内存泄漏
ps aux --sort=-%mem | head -10

# 检查交换空间
swapon -s
```

**解决方案**:

- 减少批处理大小
- 使用 CPU 模式
- 增加系统内存
- 优化模型配置

#### 1.4 请求响应超时

**问题**: 请求响应超时
**排查步骤**:

```bash
# 检查网络连接
ping localhost
telnet localhost 8002

# 检查服务状态
curl -v http://localhost:8002/health

# 检查负载
uptime
iostat 1 5
```

**解决方案**:

- 增加超时时间
- 优化模型性能
- 增加服务实例
- 检查网络配置

### 2. 调试工具

#### 2.1 健康检查脚本

```bash
#!/bin/bash
# health_check.sh

echo "=== 知识检索服务健康检查 ==="
echo "检查时间: $(date)"
echo ""

# 检查服务状态
check_service_status() {
    echo "1. 检查服务状态..."
    if curl -s http://localhost:8002/health > /dev/null; then
        echo "✅ 服务运行正常"
    else
        echo "❌ 服务无响应"
        return 1
    fi
}

# 检查响应时间
check_response_time() {
    echo "2. 检查响应时间..."
    local start_time=$(date +%s.%N)
    curl -s http://localhost:8002/health > /dev/null
    local end_time=$(date +%s.%N)
    local response_time=$(echo "$end_time - $start_time" | bc)

    if (( $(echo "$response_time < 1.0" | bc -l) )); then
        echo "✅ 响应时间正常: ${response_time}s"
    else
        echo "⚠️ 响应时间过长: ${response_time}s"
    fi
}

# 检查资源使用
check_resource_usage() {
    echo "3. 检查资源使用..."
    local cpu_usage=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
    local memory_usage=$(free | grep Mem | awk '{printf("%.2f"), $3/$2 * 100.0}')

    if (( $(echo "$cpu_usage < 80" | bc -l) )); then
        echo "✅ CPU使用率正常: ${cpu_usage}%"
    else
        echo "⚠️ CPU使用率过高: ${cpu_usage}%"
    fi

    if (( $(echo "$memory_usage < 85" | bc -l) )); then
        echo "✅ 内存使用率正常: ${memory_usage}%"
    else
        echo "⚠️ 内存使用率过高: ${memory_usage}%"
    fi
}

# 执行检查
check_service_status && check_response_time && check_resource_usage

echo ""
echo "=== 健康检查完成 ==="
```

#### 2.2 性能测试脚本

```bash
#!/bin/bash
# performance_test.sh

echo "=== 知识检索服务性能测试 ==="
echo "测试时间: $(date)"
echo ""

# 并发测试
concurrent_test() {
    echo "1. 并发测试..."
    local concurrent_users=10
    local requests_per_user=100

    echo "并发用户数: $concurrent_users"
    echo "每用户请求数: $requests_per_user"

    # 使用ab进行压力测试
    ab -n $((concurrent_users * requests_per_user)) -c $concurrent_users http://localhost:8002/health
}

# 响应时间测试
response_time_test() {
    echo "2. 响应时间测试..."
    local test_requests=1000

    echo "测试请求数: $test_requests"

    for i in $(seq 1 $test_requests); do
        local start_time=$(date +%s.%N)
        curl -s http://localhost:8002/health > /dev/null
        local end_time=$(date +%s.%N)
        local response_time=$(echo "$end_time - $start_time" | bc)
        echo "$i,$response_time"
    done | tee response_times.csv
}

# 内存泄漏测试
memory_leak_test() {
    echo "3. 内存泄漏测试..."
    local initial_memory=$(ps aux | grep python | grep retrieval | awk '{print $6}')
    echo "初始内存使用: ${initial_memory}KB"

    # 运行一段时间后检查内存
    sleep 300
    local final_memory=$(ps aux | grep python | grep retrieval | awk '{print $6}')
    echo "最终内存使用: ${final_memory}KB"

    local memory_diff=$((final_memory - initial_memory))
    if [ $memory_diff -gt 100000 ]; then
        echo "⚠️ 可能存在内存泄漏: 增加${memory_diff}KB"
    else
        echo "✅ 内存使用正常"
    fi
}

# 执行测试
concurrent_test
response_time_test
memory_leak_test

echo ""
echo "=== 性能测试完成 ==="
```

### 3. 监控脚本

#### 3.1 系统监控脚本

```bash
#!/bin/bash
# system_monitor.sh

echo "=== 知识检索服务系统监控 ==="
echo "检查时间: $(date)"
echo ""

check_service_status
check_response_time
check_resource_usage

echo ""
echo "=== 监控完成 ==="
```

---

## 🔒 安全配置

### 1. 访问控制

#### 1.1 防火墙配置

```bash
# UFW防火墙配置
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing

# 允许SSH
sudo ufw allow ssh

# 允许HTTP/HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 允许服务端口
sudo ufw allow 8002/tcp

# 查看状态
sudo ufw status verbose
```

#### 1.2 访问限制

```nginx
# Nginx访问限制
location / {
    # 限制请求频率
    limit_req zone=api burst=20 nodelay;

    # 限制连接数
    limit_conn conn_limit_per_ip 10;

    # IP白名单
    allow 192.168.1.0/24;
    allow 10.0.0.0/8;
    deny all;

    proxy_pass http://knowledge_retrieval_backend;
}

# 定义限制区域
http {
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_conn_zone $binary_remote_addr zone=conn_limit_per_ip:10m;
}
```

### 2. 数据加密

#### 2.1 SSL/TLS 配置

```nginx
# SSL配置
ssl_certificate /etc/nginx/ssl/cert.pem;
ssl_certificate_key /etc/nginx/ssl/key.pem;
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 10m;
```

#### 2.2 数据加密

```python
from cryptography.fernet import Fernet
import base64

class DataEncryption:
    def __init__(self, key: str = None):
        """初始化数据加密"""
        if key:
            self.key = key.encode()
        else:
            self.key = Fernet.generate_key()
        self.cipher = Fernet(self.key)

    def encrypt(self, data: str) -> str:
        """加密数据"""
        encrypted_data = self.cipher.encrypt(data.encode())
        return base64.b64encode(encrypted_data).decode()

    def decrypt(self, encrypted_data: str) -> str:
        """解密数据"""
        encrypted_bytes = base64.b64decode(encrypted_data.encode())
        decrypted_data = self.cipher.decrypt(encrypted_bytes)
        return decrypted_data.decode()
```

### 3. 安全加固

#### 3.1 系统加固

```bash
# 系统安全加固脚本
#!/bin/bash

echo "=== 系统安全加固 ==="

# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装安全工具
sudo apt install -y fail2ban ufw unattended-upgrades

# 配置自动更新
sudo dpkg-reconfigure -plow unattended-upgrades

# 配置SSH
sudo sed -i 's/#PermitRootLogin yes/PermitRootLogin no/' /etc/ssh/sshd_config
sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart ssh

# 配置防火墙
sudo ufw enable
sudo ufw default deny incoming
sudo ufw default allow outgoing

echo "=== 安全加固完成 ==="
```

#### 3.2 应用安全

```python
# 应用安全配置
import secrets
import hashlib
from functools import wraps

class SecurityManager:
    def __init__(self):
        """初始化安全管理器"""
        self.secret_key = secrets.token_hex(32)
        self.jwt_secret = secrets.token_hex(32)

    def generate_token(self, user_id: str) -> str:
        """生成访问令牌"""
        payload = {
            'user_id': user_id,
            'timestamp': time.time(),
            'nonce': secrets.token_hex(16)
        }
        # 实现JWT生成逻辑
        return self._create_jwt(payload)

    def validate_token(self, token: str) -> bool:
        """验证访问令牌"""
        try:
            # 实现JWT验证逻辑
            payload = self._verify_jwt(token)
            return payload is not None
        except:
            return False

    def hash_password(self, password: str) -> str:
        """哈希密码"""
        salt = secrets.token_hex(16)
        hash_obj = hashlib.pbkdf2_hmac('sha256', password.encode(), salt.encode(), 100000)
        return f"{salt}:{hash_obj.hex()}"

    def verify_password(self, password: str, hashed: str) -> bool:
        """验证密码"""
        try:
            salt, hash_hex = hashed.split(':')
            hash_obj = hashlib.pbkdf2_hmac('sha256', password.encode(), salt.encode(), 100000)
            return hash_obj.hex() == hash_hex
        except:
            return False

# 安全装饰器
def require_auth(f):
    @wraps(f)
    def decorated_function(*args, **kwargs):
        token = request.headers.get('Authorization')
        if not token or not security_manager.validate_token(token):
            return jsonify({'error': 'Unauthorized'}), 401
        return f(*args, **kwargs)
    return decorated_function
```

---

## 💾 备份恢复

### 1. 数据备份

#### 1.1 自动备份脚本

```bash
#!/bin/bash
# backup.sh

echo "=== 知识检索服务数据备份 ==="
echo "备份时间: $(date)"

# 配置
BACKUP_DIR="/backup/knowledge_retrieval"
SERVICE_DIR="/opt/knowledge_retrieval_service"
DATA_DIR="/data"
RETENTION_DAYS=30

# 创建备份目录
mkdir -p $BACKUP_DIR

# 生成备份文件名
BACKUP_NAME="knowledge_retrieval_backup_$(date +%Y%m%d_%H%M%S)"
BACKUP_PATH="$BACKUP_DIR/$BACKUP_NAME"

echo "备份路径: $BACKUP_PATH"

# 创建备份
tar -czf "$BACKUP_PATH.tar.gz" \
    -C "$SERVICE_DIR" . \
    -C "$DATA_DIR" . \
    --exclude="*.log" \
    --exclude="*.tmp" \
    --exclude="__pycache__" \
    --exclude="*.pyc"

# 检查备份文件
if [ -f "$BACKUP_PATH.tar.gz" ]; then
    echo "✅ 备份成功: $BACKUP_PATH.tar.gz"

    # 计算文件大小
    BACKUP_SIZE=$(du -h "$BACKUP_PATH.tar.gz" | cut -f1)
    echo "备份大小: $BACKUP_SIZE"

    # 清理旧备份
    find $BACKUP_DIR -name "knowledge_retrieval_backup_*.tar.gz" -mtime +$RETENTION_DAYS -delete
    echo "✅ 旧备份已清理"
else
    echo "❌ 备份失败"
    exit 1
fi

echo "=== 备份完成 ==="
```

#### 1.2 增量备份

```bash
#!/bin/bash
# incremental_backup.sh

echo "=== 增量备份 ==="

# 配置
BACKUP_DIR="/backup/knowledge_retrieval/incremental"
SERVICE_DIR="/opt/knowledge_retrieval_service"
DATA_DIR="/data"
LAST_BACKUP_FILE="$BACKUP_DIR/last_backup.txt"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 获取上次备份时间
if [ -f "$LAST_BACKUP_FILE" ]; then
    LAST_BACKUP=$(cat "$LAST_BACKUP_FILE")
else
    LAST_BACKUP="1970-01-01 00:00:00"
fi

# 生成备份文件名
BACKUP_NAME="incremental_backup_$(date +%Y%m%d_%H%M%S)"
BACKUP_PATH="$BACKUP_DIR/$BACKUP_NAME"

echo "上次备份时间: $LAST_BACKUP"
echo "当前备份时间: $(date)"

# 创建增量备份
tar -czf "$BACKUP_PATH.tar.gz" \
    --newer="$LAST_BACKUP" \
    -C "$SERVICE_DIR" . \
    -C "$DATA_DIR" . \
    --exclude="*.log" \
    --exclude="*.tmp" \
    --exclude="__pycache__" \
    --exclude="*.pyc"

# 更新上次备份时间
echo "$(date)" > "$LAST_BACKUP_FILE"

echo "✅ 增量备份完成: $BACKUP_PATH.tar.gz"
```

### 2. 数据恢复

#### 2.1 恢复脚本

```bash
#!/bin/bash
# restore.sh

echo "=== 知识检索服务数据恢复 ==="

# 配置
BACKUP_DIR="/backup/knowledge_retrieval"
SERVICE_DIR="/opt/knowledge_retrieval_service"
DATA_DIR="/data"

# 检查参数
if [ $# -eq 0 ]; then
    echo "用法: $0 <备份文件名>"
    echo "可用备份:"
    ls -la $BACKUP_DIR/*.tar.gz
    exit 1
fi

BACKUP_FILE="$1"
if [ ! -f "$BACKUP_FILE" ]; then
    echo "❌ 备份文件不存在: $BACKUP_FILE"
    exit 1
fi

echo "恢复备份: $BACKUP_FILE"

# 停止服务
echo "停止服务..."
systemctl stop knowledge-retrieval-service

# 备份当前数据
echo "备份当前数据..."
CURRENT_BACKUP="current_backup_$(date +%Y%m%d_%H%M%S).tar.gz"
tar -czf "$BACKUP_DIR/$CURRENT_BACKUP" \
    -C "$SERVICE_DIR" . \
    -C "$DATA_DIR" .

# 恢复数据
echo "恢复数据..."
tar -xzf "$BACKUP_FILE" -C /

# 启动服务
echo "启动服务..."
systemctl start knowledge-retrieval-service

# 验证恢复
echo "验证恢复..."
sleep 10
if curl -s http://localhost:8002/health > /dev/null; then
    echo "✅ 恢复成功"
else
    echo "❌ 恢复失败，回滚到当前备份"
    tar -xzf "$BACKUP_DIR/$CURRENT_BACKUP" -C /
    systemctl start knowledge-retrieval-service
fi

echo "=== 恢复完成 ==="
```

#### 2.2 灾难恢复

```bash
#!/bin/bash
# disaster_recovery.sh

echo "=== 灾难恢复 ==="

# 配置
BACKUP_SERVER="backup.example.com"
BACKUP_USER="backup"
BACKUP_DIR="/backup/knowledge_retrieval"
LOCAL_DIR="/opt/knowledge_retrieval_service"

# 从远程服务器下载最新备份
echo "下载最新备份..."
LATEST_BACKUP=$(ssh $BACKUP_USER@$BACKUP_SERVER "ls -t $BACKUP_DIR/*.tar.gz | head -1")
scp $BACKUP_USER@$BACKUP_SERVER:$LATEST_BACKUP /tmp/

# 恢复数据
echo "恢复数据..."
tar -xzf /tmp/$(basename $LATEST_BACKUP) -C /

# 启动服务
echo "启动服务..."
systemctl start knowledge-retrieval-service

# 验证恢复
echo "验证恢复..."
sleep 30
if curl -s http://localhost:8002/health > /dev/null; then
    echo "✅ 灾难恢复成功"
else
    echo "❌ 灾难恢复失败"
    exit 1
fi

echo "=== 灾难恢复完成 ==="
```

---

## 📈 性能优化

### 1. 系统优化

#### 1.1 内核参数优化

```bash
# /etc/sysctl.conf
# 网络优化
net.core.rmem_max = 16777216
net.core.wmem_max = 16777216
net.ipv4.tcp_rmem = 4096 87380 16777216
net.ipv4.tcp_wmem = 4096 65536 16777216
net.ipv4.tcp_congestion_control = bbr

# 文件系统优化
fs.file-max = 65536
vm.swappiness = 10
vm.dirty_ratio = 15
vm.dirty_background_ratio = 5

# 应用优化
kernel.pid_max = 4194304
kernel.threads-max = 4194304
```

#### 1.2 系统资源优化

```bash
# 限制文件描述符
echo "* soft nofile 65536" >> /etc/security/limits.conf
echo "* hard nofile 65536" >> /etc/security/limits.conf

# 优化内存管理
echo "vm.swappiness=10" >> /etc/sysctl.conf
echo "vm.dirty_ratio=15" >> /etc/sysctl.conf
echo "vm.dirty_background_ratio=5" >> /etc/sysctl.conf

# 应用配置
sysctl -p
```

### 2. 应用优化

#### 2.1 代码优化

```python
# 性能优化示例
import asyncio
import aiohttp
from concurrent.futures import ThreadPoolExecutor

class OptimizedService:
    def __init__(self):
        """初始化优化服务"""
        self.executor = ThreadPoolExecutor(max_workers=4)
        self.session = None

    async def init_session(self):
        """初始化HTTP会话"""
        self.session = aiohttp.ClientSession(
            connector=aiohttp.TCPConnector(limit=100, limit_per_host=30),
            timeout=aiohttp.ClientTimeout(total=30)
        )

    async def batch_process(self, items: List[str]) -> List[str]:
        """批量处理"""
        tasks = [self.process_item(item) for item in items]
        results = await asyncio.gather(*tasks, return_exceptions=True)
        return [r for r in results if not isinstance(r, Exception)]

    async def process_item(self, item: str) -> str:
        """处理单个项目"""
        # 异步处理逻辑
        await asyncio.sleep(0.1)
        return f"processed_{item}"

    def cpu_intensive_task(self, data: str) -> str:
        """CPU密集型任务"""
        # 在线程池中执行CPU密集型任务
        return f"cpu_result_{data}"

    async def mixed_task(self, data: str) -> str:
        """混合任务"""
        # 在线程池中执行CPU密集型部分
        cpu_result = await asyncio.get_event_loop().run_in_executor(
            self.executor, self.cpu_intensive_task, data
        )

        # 异步执行IO密集型部分
        io_result = await self.process_item(cpu_result)

        return io_result
```

#### 2.2 缓存优化

```python
import redis
import json
from functools import wraps

class CacheManager:
    def __init__(self, redis_url: str = "redis://localhost:6379"):
        """初始化缓存管理器"""
        self.redis_client = redis.from_url(redis_url)
        self.default_ttl = 3600  # 1小时

    def cache_result(self, ttl: int = None):
        """缓存结果装饰器"""
        def decorator(func):
            @wraps(func)
            async def wrapper(*args, **kwargs):
                # 生成缓存键
                cache_key = f"{func.__name__}:{hash(str(args) + str(kwargs))}"

                # 尝试从缓存获取
                cached_result = self.redis_client.get(cache_key)
                if cached_result:
                    return json.loads(cached_result)

                # 执行函数
                result = await func(*args, **kwargs)

                # 缓存结果
                self.redis_client.setex(
                    cache_key,
                    ttl or self.default_ttl,
                    json.dumps(result, default=str)
                )

                return result
            return wrapper
        return decorator

    def invalidate_cache(self, pattern: str):
        """清除缓存"""
        keys = self.redis_client.keys(pattern)
        if keys:
            self.redis_client.delete(*keys)

    def get_cache_stats(self) -> dict:
        """获取缓存统计"""
        info = self.redis_client.info()
        return {
            'used_memory': info['used_memory_human'],
            'connected_clients': info['connected_clients'],
            'total_commands_processed': info['total_commands_processed'],
            'keyspace_hits': info['keyspace_hits'],
            'keyspace_misses': info['keyspace_misses']
        }
```

### 3. 数据库优化

#### 3.1 ChromaDB 优化

```python
import chromadb
from chromadb.config import Settings

class OptimizedChromaDB:
    def __init__(self, persist_directory: str = "./chroma_db"):
        """初始化优化的ChromaDB"""
        self.client = chromadb.PersistentClient(
            path=persist_directory,
            settings=Settings(
                anonymized_telemetry=False,
                allow_reset=True
            )
        )

        # 创建集合
        self.collection = self.client.get_or_create_collection(
            name="medical_knowledge",
            metadata={"hnsw:space": "cosine"}
        )

    def optimize_collection(self):
        """优化集合"""
        # 设置索引参数
        self.collection.modify(
            metadata={"hnsw:space": "cosine", "hnsw:construction_ef": 200}
        )

    def batch_operations(self, operations: List[dict]):
        """批量操作"""
        # 批量添加
        add_operations = [op for op in operations if op['type'] == 'add']
        if add_operations:
            self.collection.add(
                documents=[op['document'] for op in add_operations],
                metadatas=[op['metadata'] for op in add_operations],
                ids=[op['id'] for op in add_operations]
            )

        # 批量更新
        update_operations = [op for op in operations if op['type'] == 'update']
        if update_operations:
            self.collection.update(
                documents=[op['document'] for op in update_operations],
                metadatas=[op['metadata'] for op in update_operations],
                ids=[op['id'] for op in update_operations]
            )

    def get_collection_stats(self) -> dict:
        """获取集合统计"""
        count = self.collection.count()
        return {
            'total_documents': count,
            'collection_name': self.collection.name,
            'metadata': self.collection.metadata
        }
```

#### 3.2 Redis 优化

```bash
# redis.conf
# 内存优化
maxmemory 2gb
maxmemory-policy allkeys-lru

# 持久化优化
save 900 1
save 300 10
save 60 10000

# 网络优化
tcp-keepalive 300
timeout 0

# 日志优化
loglevel notice
logfile /var/log/redis/redis-server.log
```

---

## 📊 监控指标

### 1. 服务指标

#### 1.1 性能指标

- **响应时间**: 平均响应时间 < 3 秒
- **吞吐量**: 每秒处理请求数 > 10
- **错误率**: 错误率 < 1%
- **可用性**: 服务可用性 > 99.9%

#### 1.2 系统指标

- **CPU 使用率**: < 80%
- **内存使用率**: < 85%
- **磁盘使用率**: < 90%
- **GPU 使用率**: < 90%

#### 1.3 业务指标

- **检索准确率**: > 85%
- **生成质量**: > 4.0/5.0
- **用户满意度**: > 4.0/5.0
- **并发用户数**: 支持 > 100
- **日处理量**: > 10000 次

### 2. 监控脚本

#### 2.1 综合监控脚本

```bash
#!/bin/bash
# comprehensive_monitor.sh

echo "=== 知识检索服务综合监控 ==="
echo "监控时间: $(date)"
echo ""

# 服务状态检查
check_service_status() {
    echo "1. 服务状态检查..."
    if systemctl is-active --quiet knowledge-retrieval-service; then
        echo "✅ 服务运行正常"
    else
        echo "❌ 服务未运行"
        return 1
    fi
}

# 响应时间检查
check_response_time() {
    echo "2. 响应时间检查..."
    local start_time=$(date +%s.%N)
    curl -s http://localhost:8002/health > /dev/null
    local end_time=$(date +%s.%N)
    local response_time=$(echo "$end_time - $start_time" | bc)

    if (( $(echo "$response_time < 1.0" | bc -l) )); then
        echo "✅ 响应时间正常: ${response_time}s"
    else
        echo "⚠️ 响应时间过长: ${response_time}s"
    fi
}

# 资源使用检查
check_resource_usage() {
    echo "3. 资源使用检查..."
    local cpu_usage=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
    local memory_usage=$(free | grep Mem | awk '{printf("%.2f"), $3/$2 * 100.0}')

    if (( $(echo "$cpu_usage < 80" | bc -l) )); then
        echo "✅ CPU使用率正常: ${cpu_usage}%"
    else
        echo "⚠️ CPU使用率过高: ${cpu_usage}%"
    fi

    if (( $(echo "$memory_usage < 85" | bc -l) )); then
        echo "✅ 内存使用率正常: ${memory_usage}%"
    else
        echo "⚠️ 内存使用率过高: ${memory_usage}%"
    fi
}

# 磁盘空间检查
check_disk_space() {
    echo "4. 磁盘空间检查..."
    local disk_usage=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')

    if [ $disk_usage -lt 90 ]; then
        echo "✅ 磁盘使用率正常: ${disk_usage}%"
    else
        echo "⚠️ 磁盘使用率过高: ${disk_usage}%"
    fi
}

# 网络连接检查
check_network() {
    echo "5. 网络连接检查..."
    if ping -c 1 8.8.8.8 > /dev/null 2>&1; then
        echo "✅ 网络连接正常"
    else
        echo "❌ 网络连接异常"
    fi
}

# 日志检查
check_logs() {
    echo "6. 日志检查..."
    local error_count=$(journalctl -u knowledge-retrieval-service --since "1 hour ago" | grep -i error | wc -l)

    if [ $error_count -eq 0 ]; then
        echo "✅ 无错误日志"
    else
        echo "⚠️ 发现 ${error_count} 个错误日志"
    fi
}

# 执行所有检查
check_service_status
check_response_time
check_resource_usage
check_disk_space
check_network
check_logs

echo ""
echo "=== 监控完成 ==="
```

---

## 🔮 未来规划

### 1. 技术升级

- **微服务架构**: 进一步拆分服务，提高可扩展性
- **容器编排**: 使用 Kubernetes 进行容器编排
- **服务网格**: 引入 Istio 等服务网格技术
- **边缘计算**: 支持边缘设备部署

### 2. 功能增强

- **多语言支持**: 支持更多语言的检索和生成
- **实时学习**: 支持在线学习和模型更新
- **个性化推荐**: 基于用户行为的个性化推荐
- **可视化界面**: 提供可视化的管理界面

### 3. 运维优化

- **自动化运维**: 实现自动化部署和运维
- **智能监控**: 基于 AI 的智能监控和告警
- **预测性维护**: 预测性维护和故障预防
- **成本优化**: 资源使用成本优化

---

## 📋 部署检查清单

### 基础检查

- [ ] Python 环境正确安装
- [ ] 所有依赖包已安装
- [ ] 数据文件完整
- [ ] 配置文件正确
- [ ] 端口未被占用
- [ ] 权限设置正确

### 功能检查

- [ ] 文本检索正常工作
- [ ] 图像检索正常工作
- [ ] 向量数据库构建成功
- [ ] 统一检索接口可用
- [ ] 错误处理机制正常
- [ ] 流式响应功能正常

### 性能检查

- [ ] 检索响应时间合理（<2 秒）
- [ ] 内存使用正常
- [ ] 并发处理能力
- [ ] 负载均衡配置
- [ ] 缓存机制有效

### 生产环境检查

- [ ] Docker 服务运行正常
- [ ] 数据库连接正常
- [ ] 监控和日志配置
- [ ] 备份和恢复机制
- [ ] 安全配置完整
- [ ] SSL 证书配置

---

## 🎉 总结

### 部署运维优势

✅ **完整方案**：提供完整的部署和运维方案  
✅ **多种部署**：支持多种部署方式  
✅ **监控完善**：全面的监控和告警系统  
✅ **安全可靠**：完善的安全配置和备份机制  
✅ **性能优化**：多种性能优化策略  
✅ **易于维护**：简化的维护和故障排除

### 核心成就

1. **完整部署方案**：从开发到生产的完整部署流程
2. **监控体系**：全面的监控和告警系统
3. **安全机制**：完善的安全配置和访问控制
4. **备份恢复**：可靠的数据备份和恢复机制
5. **性能优化**：多种性能优化策略

### 技术特色

- **容器化部署**：Docker 和 Kubernetes 支持
- **负载均衡**：Nginx 和 HAProxy 配置
- **监控告警**：Prometheus 和 Grafana 集成
- **安全加固**：SSL/TLS 和访问控制
- **自动化运维**：脚本化和自动化运维

---

**版本**：v2.2.0  
**更新时间**：2024 年 12 月  
**维护团队**：智诊通开发团队  
**文档状态**：完整、清晰、合理、与代码同步

_本文档详细说明了智诊通知识检索服务的部署运维方案，包含了环境准备、部署方案、配置管理、监控运维、安全配置、备份恢复、性能优化、故障排除、未来规划等全面内容。_
