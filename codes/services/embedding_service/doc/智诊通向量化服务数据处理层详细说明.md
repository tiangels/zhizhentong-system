# 智诊通向量化服务数据处理层详细说明

## 📋 模块概述

数据处理层是智诊通向量化服务的核心组件，负责对各种医疗数据进行清洗、预处理和格式转换。该层基于统一的数据处理架构，支持文本、图像、语音等多种数据类型的处理。

### 🎯 核心功能

- **统一数据处理架构**: 基于 BaseDataProcessor 的统一处理框架
- **多模态数据处理**: 支持文本、图像、语音等多种数据类型
- **医疗数据清洗**: 专门针对医疗数据的清洗和预处理
- **数据脱敏处理**: 自动识别和脱敏敏感信息
- **医学符号保留**: 保留重要的医学符号和单位
- **质量检查**: 自动进行数据质量评估
- **结构化输出**: 生成标准化的 JSON 格式数据

### 🏗️ 架构设计

```
数据处理层
├── BaseDataProcessor (基础处理器)
├── TextPreprocessor (文本处理器)
├── ImageTextPreprocessor (图像文本处理器)
├── VoicePreprocessor (语音处理器)
└── DataPipeline (数据管道)
```

## 🔧 核心文件详解

### 1. BaseDataProcessor - 统一数据处理基类

#### 1.1 文件概述

- **文件路径**: `processors/base_processor.py`
- **主要功能**: 提供统一的数据处理基类，包含通用的数据清洗、脱敏、医学符号保留等功能
- **技术栈**: Python, Pandas, Regex, Logging

#### 1.2 核心组件

##### 1.2.1 初始化配置

```python
def __init__(self, raw_data_dir: str, output_dir: str, processor_name: str = "BaseProcessor"):
    self.raw_data_dir = Path(raw_data_dir)
    self.output_dir = Path(output_dir)
    self.processor_name = processor_name

    # 初始化通用组件
    self._init_data_cleaning()
    self._init_medical_symbols()
    self._init_desensitization()
    self._init_quality_checker()
```

**配置说明**:

- **raw_data_dir**: 原始数据目录路径
- **output_dir**: 输出目录路径
- **processor_name**: 处理器名称标识

##### 1.2.2 数据清洗组件

```python
def _init_data_cleaning(self):
    # 乱码检测模式 - 保护医学符号
    self.garbled_patterns = [
        r'[\x00-\x08\x0b\x0c\x0e-\x1f\x7f-\x9f]',  # 只删除控制字符
        r'[^\w\s\u4e00-\u9fff\u3000-\u303f\uff00-\uffef.,;:!?()（）【】《》""''、。，；：！？℃°↑↓±/]'  # 保留医学符号
    ]

    # 广告水印模式
    self.ad_patterns = [
        r'广告|推广|营销|促销|优惠|折扣|免费|试用',
        r'联系我们|咨询热线|客服|电话|微信|QQ',
        r'版权所有|Copyright|©|®|™',
        r'www\.|http://|https://|\.com|\.cn|\.net',
        r'关注我们|扫码|二维码|公众号|订阅'
    ]
```

**功能说明**:

- **乱码清理**: 删除控制字符，保留医学符号
- **广告清理**: 识别和删除广告内容
- **页眉页脚清理**: 删除文档页眉页脚信息

##### 1.2.3 医学符号保留组件

```python
def _init_medical_symbols(self):
    # 医学单位符号
    self.medical_units = {
        'temperature': ['℃', '°C', '华氏度', '℉', '°F'],
        'percentage': ['%', '百分比', '百分率'],
        'weight': ['mg', 'g', 'kg', '毫克', '克', '千克', '斤', '两'],
        'volume': ['ml', 'l', '毫升', '升', 'cc'],
        'pressure': ['mmHg', 'kPa', '毫米汞柱', '千帕'],
        'frequency': ['次/分', '次/日', '次/周', '次/月', '次/年'],
        'concentration': ['mg/dl', 'mmol/l', 'μmol/l', 'ng/ml', 'μg/ml']
    }

    # 检验符号
    self.test_symbols = {
        'positive': ['+', '阳性', 'positive', 'pos', 'P'],
        'negative': ['-', '阴性', 'negative', 'neg', 'N'],
        'normal': ['正常', 'normal', 'N', 'norm'],
        'abnormal': ['异常', 'abnormal', 'A', 'abn'],
        'high': ['↑', '↑↑', '↑↑↑', '高', 'high', 'H'],
        'low': ['↓', '↓↓', '↓↓↓', '低', 'low', 'L'],
        'trace': ['±', '±±', '微量', 'trace', 'T']
    }
```

**功能说明**:

- **医学单位保留**: 保留温度、重量、体积等医学单位
- **检验符号保留**: 保留阳性、阴性、正常、异常等检验符号
- **医学标点保留**: 保留医学文档中的特殊标点符号

##### 1.2.4 数据脱敏组件

```python
def _init_desensitization(self):
    # 身份证号模式
    self.id_pattern = r'\b\d{17}[\dXx]\b'

    # 手机号模式
    self.phone_pattern = r'\b1[3-9]\d{9}\b'

    # 姓名模式（简单中文姓名）
    self.name_pattern = r'[王李张刘陈杨黄赵吴周徐孙马朱胡郭何高林罗郑梁谢宋唐许韩冯邓曹彭曾萧田董袁潘于蒋蔡余杜叶程魏苏吕丁任沈姚卢姜崔钟谭陆汪范金石廖贾夏韦付方白邹孟熊秦邱江尹薛闫段雷侯龙史陶黎贺顾毛郝龚邵万钱严覃武戴莫孔向汤][\u4e00-\u9fff]{1,3}'

    # 病历号模式
    self.medical_record_pattern = r'\b[A-Za-z0-9]{6,20}\b'

    # 地址模式
    self.address_pattern = r'[省市区县乡镇街道村组号]\d*[号室楼单元]'
```

**功能说明**:

- **身份证脱敏**: 识别并脱敏身份证号码
- **手机号脱敏**: 识别并脱敏手机号码
- **姓名脱敏**: 识别并脱敏中文姓名
- **病历号脱敏**: 识别并脱敏病历号
- **地址脱敏**: 识别并脱敏地址信息

#### 1.3 核心方法

##### 1.3.1 文本清洗方法

```python
def clean_text(self, text: str) -> Dict[str, Any]:
    """
    清洗文本数据

    Args:
        text: 原始文本

    Returns:
        清洗结果字典
    """
    # 1. 检测和清理乱码
    # 2. 清理广告水印
    # 3. 清理页眉页脚
    # 4. 清理多余空白
    # 5. 检查文本长度
    # 6. 检查医学内容

    return {
        'cleaned_text': text,
        'quality_score': quality_score,
        'issues': issues,
        'original_length': len(original_text),
        'cleaned_length': len(text),
        'medical_terms_count': medical_terms_count
    }
```

**处理流程**:

1. 检测和清理乱码字符
2. 清理广告和水印内容
3. 清理页眉页脚信息
4. 清理多余空白字符
5. 检查文本长度是否符合要求
6. 统计医学术语数量

##### 1.3.2 数据脱敏方法

```python
def desensitize_text(self, text: str) -> Dict[str, Any]:
    """
    数据脱敏处理

    Args:
        text: 原始文本

    Returns:
        脱敏结果字典
    """
    # 1. 身份证号脱敏
    # 2. 手机号脱敏
    # 3. 姓名脱敏
    # 4. 病历号脱敏
    # 5. 地址脱敏

    return {
        'desensitized_text': desensitized_text,
        'sensitive_info': sensitive_info,
        'has_sensitive_data': bool(sensitive_info)
    }
```

**处理流程**:

1. 识别身份证号码并进行脱敏
2. 识别手机号码并进行脱敏
3. 识别中文姓名并进行脱敏
4. 识别病历号并进行脱敏
5. 识别地址信息并进行脱敏

##### 1.3.3 结构化输出方法

```python
def create_structured_output(self, content: str, metadata: Dict[str, Any]) -> Dict[str, Any]:
    """
    创建结构化输出格式

    Args:
        content: 文本内容
        metadata: 元数据

    Returns:
        结构化输出字典
    """
    # 生成唯一ID
    content_hash = hashlib.md5(content.encode('utf-8')).hexdigest()[:16]

    # 创建结构化输出
    structured_output = {
        'id': f"doc_{content_hash}",
        'content': content,
        'metadata': {
            'timestamp': datetime.now().isoformat(),
            'content_length': len(content),
            'content_type': 'text',
            'processing_version': '1.0',
            'processor_name': self.processor_name,
            **metadata
        },
        'quality_metrics': {
            'medical_terms_count': self._count_medical_terms(content),
            'has_medical_symbols': any(symbol in content for symbols in self.medical_units.values() for symbol in symbols),
            'has_test_symbols': any(symbol in content for symbols in self.test_symbols.values() for symbol in symbols)
        }
    }

    return structured_output
```

**功能说明**:

- **唯一标识**: 为每个文档生成唯一 ID
- **元数据管理**: 记录处理时间、内容长度等信息
- **质量指标**: 统计医学术语数量和符号使用情况

### 2. TextPreprocessor - 医疗文本数据预处理器

#### 2.1 文件概述

- **文件路径**: `processors/text_preprocessing.py`
- **主要功能**: 专门处理文本文件（PDF、TXT、CSV、JSON、Excel 等），进行医疗文本数据清洗和预处理
- **技术栈**: Python, Pandas, PyPDF2, pdfplumber, openpyxl, jieba

#### 2.2 核心组件

##### 2.2.1 支持的文件格式

```python
self.supported_formats = {
    '.txt': self._process_txt_file,
    '.csv': self._process_csv_file,
    '.json': self._process_json_file,
    '.pdf': self._process_pdf_file,
    '.xlsx': self._process_excel_file,
    '.xls': self._process_excel_file,
}
```

**支持格式**:

- **TXT**: 纯文本文件
- **CSV**: 逗号分隔值文件
- **JSON**: JSON 格式文件
- **PDF**: PDF 文档文件
- **Excel**: Excel 电子表格文件

##### 2.2.2 数据类型检测

```python
def _detect_data_type_from_filename(self, filename: str) -> str:
    """根据文件名判断数据类型"""
    filename_lower = filename.lower()

    # 医疗对话数据
    if any(keyword in filename_lower for keyword in ['dialogue', '对话', 'chat', 'conversation']):
        return 'dialogue'

    # VQA数据
    elif any(keyword in filename_lower for keyword in ['vqa', 'question', 'answer', '问答']):
        return 'vqa'

    # 医疗文档
    elif any(keyword in filename_lower for keyword in ['medical', '医疗', 'health', 'healthcare', 'diagnosis', '诊断']):
        return 'medical_document'

    # 默认为通用文档
    else:
        return 'general_document'
```

**数据类型**:

- **dialogue**: 医疗对话数据
- **vqa**: 问答数据
- **medical_document**: 医疗文档
- **general_document**: 通用文档

#### 2.3 核心处理方法

##### 2.3.1 TXT 文件处理

```python
def _process_txt_file(self, file_path: Path) -> List[Dict[str, Any]]:
    """处理TXT文件"""
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            content = f.read()

        # 按段落分割
        paragraphs = [p.strip() for p in content.split('\n\n') if p.strip()]

        documents = []
        for i, paragraph in enumerate(paragraphs):
            if len(paragraph) > 10:  # 过滤太短的段落
                # 1. 调用基类的文本清洗功能，保留医学符号
                cleaned_result = self.clean_text(paragraph)

                # 2. 调用数据脱敏功能
                desensitized_result = self.desensitize_text(cleaned_result['cleaned_text'])

                # 3. 创建结构化输出
                metadata = {
                    'file_name': file_path.name,
                    'file_path': str(file_path),
                    'data_type': self._detect_data_type_from_filename(file_path.name),
                    'paragraph_index': i,
                    'quality_score': cleaned_result['quality_score'],
                    'medical_terms_count': cleaned_result['medical_terms_count'],
                    'sensitive_info': desensitized_result['sensitive_info'],
                    'has_sensitive_data': desensitized_result['has_sensitive_data']
                }

                structured_doc = self.create_structured_output(
                    desensitized_result['desensitized_text'],
                    metadata
                )

                documents.append(structured_doc)

        return documents
    except Exception as e:
        self.logger.error(f"处理TXT文件失败 {file_path}: {e}")
        return []
```

**处理流程**:

1. 读取 TXT 文件内容
2. 按段落分割文本
3. 过滤过短的段落
4. 调用基类清洗和脱敏功能
5. 创建结构化输出

##### 2.3.2 PDF 文件处理

```python
def _process_pdf_file(self, file_path) -> List[Dict[str, Any]]:
    """处理PDF文件"""
    if not PDF_AVAILABLE:
        self.logger.warning(f"PDF处理库未安装，跳过文件: {file_path}")
        return []

    try:
        documents = []

        # 使用pdfplumber提取文本
        with pdfplumber.open(file_path) as pdf:
            for page_num, page in enumerate(pdf.pages):
                text = page.extract_text()
                if text and text.strip():
                    # 1. 调用基类的文本清洗功能，保留医学符号
                    cleaned_result = self.clean_text(text.strip())

                    # 2. 调用数据脱敏功能
                    desensitized_result = self.desensitize_text(cleaned_result['cleaned_text'])

                    # 3. 创建结构化输出
                    metadata = {
                        'file_name': file_path.name,
                        'file_path': str(file_path),
                        'data_type': self._detect_data_type_from_filename(file_path.name),
                        'page_number': page_num + 1,
                        'quality_score': cleaned_result['quality_score'],
                        'medical_terms_count': cleaned_result['medical_terms_count'],
                        'sensitive_info': desensitized_result['sensitive_info'],
                        'has_sensitive_data': desensitized_result['has_sensitive_data']
                    }

                    structured_doc = self.create_structured_output(
                        desensitized_result['desensitized_text'],
                        metadata
                    )

                    documents.append(structured_doc)

        return documents
    except Exception as e:
        self.logger.error(f"处理PDF文件失败 {file_path}: {e}")
        return []
```

**处理流程**:

1. 检查 PDF 处理库是否可用
2. 使用 pdfplumber 逐页提取文本
3. 对每页文本进行清洗和脱敏
4. 创建结构化输出

##### 2.3.3 Excel 文件处理

```python
def _process_excel_file(self, file_path: Path) -> List[Dict[str, Any]]:
    """处理Excel文件"""
    if not EXCEL_AVAILABLE:
        self.logger.warning(f"Excel处理库未安装，跳过文件: {file_path}")
        return []

    try:
        documents = []

        # 读取Excel文件
        excel_file = pd.ExcelFile(file_path)

        for sheet_name in excel_file.sheet_names:
            df = pd.read_excel(file_path, sheet_name=sheet_name)

            for index, row in df.iterrows():
                # 将行数据转换为文本
                text_parts = []
                for col, value in row.items():
                    if pd.notna(value) and str(value).strip():
                        text_parts.append(f"{col}: {str(value).strip()}")

                if text_parts:
                    text_content = " | ".join(text_parts)
                    # 1. 调用基类的文本清洗功能，保留医学符号
                    cleaned_result = self.clean_text(text_content)

                    # 2. 调用数据脱敏功能
                    desensitized_result = self.desensitize_text(cleaned_result['cleaned_text'])

                    # 3. 创建结构化输出
                    metadata = {
                        'file_name': file_path.name,
                        'file_path': str(file_path),
                        'data_type': self._detect_data_type_from_filename(file_path.name),
                        'sheet_name': sheet_name,
                        'row_index': index,
                        'quality_score': cleaned_result['quality_score'],
                        'medical_terms_count': cleaned_result['medical_terms_count'],
                        'sensitive_info': desensitized_result['sensitive_info'],
                        'has_sensitive_data': desensitized_result['has_sensitive_data']
                    }

                    structured_doc = self.create_structured_output(
                        desensitized_result['desensitized_text'],
                        metadata
                    )

                    documents.append(structured_doc)

        return documents
    except Exception as e:
        self.logger.error(f"处理Excel文件失败 {file_path}: {e}")
        return []
```

**处理流程**:

1. 检查 Excel 处理库是否可用
2. 读取 Excel 文件的所有工作表
3. 将每行数据转换为文本格式
4. 进行清洗和脱敏处理
5. 创建结构化输出

#### 2.4 批量处理功能

##### 2.4.1 文件扫描

```python
def scan_text_files(self) -> List[Path]:
    """扫描文本文件"""
    text_files = []

    if self.raw_data_dir.exists():
        for file_path in self.raw_data_dir.rglob('*'):
            if file_path.is_file() and file_path.suffix.lower() in self.supported_formats:
                text_files.append(file_path)

    self.logger.info(f"🔍 发现 {len(text_files)} 个文本文件")
    return text_files
```

##### 2.4.2 批量处理

```python
def process_all_files(self, sample_size: Optional[int] = None) -> List[Dict[str, Any]]:
    """处理所有文件"""
    all_documents = []

    # 扫描文件
    text_files = self.scan_text_files()

    if not text_files:
        self.logger.warning("⚠️ 未发现文本文件")
        return []

    # 限制处理数量
    if sample_size and sample_size > 0:
        text_files = text_files[:sample_size]
        self.logger.info(f"📊 限制处理数量: {len(text_files)} 个文件")

    # 处理文件
    for file_path in tqdm(text_files, desc="处理文本文件"):
        try:
            file_ext = file_path.suffix.lower()
            if file_ext in self.supported_formats:
                self.logger.info(f"📄 处理文件: {file_path.name}")
                documents = self.supported_formats[file_ext](file_path)
                all_documents.extend(documents)
            else:
                self.logger.debug(f"跳过不支持的文件格式: {file_path.name} ({file_ext})")
        except Exception as e:
            self.logger.error(f"处理文件失败 {file_path}: {e}")
            self.failed_files.append({
                'file_name': file_path.name,
                'file_path': str(file_path),
                'error': str(e),
                'processed_time': datetime.now().isoformat()
            })

    return all_documents
```

**功能说明**:

- **文件扫描**: 递归扫描目录中的所有支持格式文件
- **批量处理**: 使用进度条显示处理进度
- **错误处理**: 记录处理失败的文件和错误信息
- **样本限制**: 支持限制处理文件数量

### 3. ImageTextPreprocessor - 医疗图文数据预处理器

#### 3.1 文件概述

- **文件路径**: `processors/image_text_preprocessing.py`
- **主要功能**: 处理医疗图文数据，包括图像预处理和文本提取，生成图文对应的结构化数据
- **技术栈**: Python, OpenCV, PIL, Pandas, NumPy, Matplotlib

#### 3.2 核心组件

##### 3.2.1 初始化配置

```python
def __init__(self, data_dir):
    # 设置输出目录
    import os
    current_dir = os.path.dirname(os.path.abspath(__file__))
    project_root = os.path.dirname(os.path.dirname(os.path.dirname(os.path.dirname(current_dir))))
    output_dir = os.path.join(project_root, "datas", "medical_knowledge", "image_text_data", "processed")
    super().__init__(data_dir, output_dir, "MedicalImageTextPreprocessor")

    self.data_dir = data_dir
    self.images_dir = os.path.join(data_dir, "chestX-rays", "images")
    self.reports_file = os.path.join(data_dir, "chestX-rays", "indiana_reports.csv")

    # 图像预处理参数
    self.image_size = (224, 224)
    self.transform = transforms.Compose([
        transforms.ToPILImage(),
        transforms.Resize(self.image_size),
        transforms.RandomHorizontalFlip(),
        transforms.ToTensor(),
        transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])
    ])
```

**配置说明**:

- **data_dir**: 原始数据目录
- **images_dir**: 图像文件目录
- **reports_file**: 报告文件路径
- **image_size**: 图像预处理尺寸
- **transform**: 图像变换管道

##### 3.2.2 报告数据加载

```python
def load_reports(self):
    """加载报告数据"""
    print(f"加载报告数据: {self.reports_file}")
    self.reports_df = pd.read_csv(self.reports_file)
    print(f"共加载 {len(self.reports_df)} 条报告数据")
    return self.reports_df
```

**功能说明**:

- 从 CSV 文件加载医疗报告数据
- 包含图像路径、检查结果、诊断印象等信息

##### 3.2.3 报告文本清洗

```python
def clean_reports(self):
    """清洗报告文本"""
    print("清洗报告文本...")
    # 去除缺失值
    self.reports_df = self.reports_df.dropna(subset=['image', 'findings', 'impression'])
    print(f"去除缺失值后: {len(self.reports_df)}")

    # 去除特殊字符
    for col in ['findings', 'impression', 'MeSH', 'Problems']:
        if col in self.reports_df.columns:
            self.reports_df[col] = self.reports_df[col].str.replace('[^\w\s\u4e00-\u9fa5]', '', regex=True)

    # 中文分词
    print("对报告文本进行分词...")
    for col in ['findings', 'impression']:
        self.reports_df[f'{col}_tokens'] = self.reports_df[col].apply(
            lambda x: ' '.join(jieba.cut(x)) if isinstance(x, str) else ""
        )
```

**处理流程**:

1. 去除缺失值
2. 清理特殊字符
3. 中文分词处理

#### 3.3 核心处理方法

##### 3.3.1 图像数据处理

```python
def process_images(self):
    """处理图像数据"""
    print("处理图像数据...")
    processed_images = []
    valid_indices = []

    # 检查图像目录是否存在
    print(f"图像目录: {self.images_dir}")
    if not os.path.exists(self.images_dir):
        print(f"错误: 图像目录不存在 {self.images_dir}")
        return

    # 创建一个图像文件名到路径的映射，以便快速查找
    image_file_map = {}
    for ext in ['.png', '.jpg', '.jpeg', '.gif', '.bmp', '.tiff']:
        for file_name in files:
            if file_name.lower().endswith(ext):
                # 提取文件名中的数字部分（假设格式为 {number}_IM-...）
                parts = file_name.split('_')
                if len(parts) > 0 and parts[0].isdigit():
                    number = parts[0]
                    if number not in image_file_map:
                        image_file_map[number] = os.path.join(self.images_dir, file_name)

    print(f"已建立 {len(image_file_map)} 个图像文件映射")

    valid_rows = []
    for idx, row in tqdm(self.reports_df.iterrows(), total=len(self.reports_df)):
        uid = str(row['uid'])
        print(f"尝试查找uid为 {uid} 的图像")

        # 首先尝试直接匹配uid
        if uid in image_file_map:
            image_path = image_file_map[uid]
            print(f"找到匹配的图像: {image_path}")
        else:
            # 如果没有直接匹配，尝试查找包含uid的文件名
            image_path = None
            for file_name in files:
                if uid in file_name:
                    image_path = os.path.join(self.images_dir, file_name)
                    print(f"找到包含uid的图像: {image_path}")
                    break

        if image_path and os.path.exists(image_path):
            # 加载图像
            image = cv2.imread(image_path)
            if image is not None:
                # 转换为RGB
                image = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)
                # 应用预处理
                processed_image = self.transform(image)
                processed_images.append(processed_image.numpy())
                valid_rows.append(row)
                print(f"成功加载并处理图像: {image_path}")
            else:
                print(f"警告: 无法加载图像 {image_path}")
        else:
            print(f"警告: 未找到uid为 {uid} 的图像文件")

    # 只保留有有效图像的报告
    if valid_rows:
        self.reports_df = pd.DataFrame(valid_rows).reset_index(drop=True)
    else:
        self.reports_df = pd.DataFrame()
    self.processed_images = np.array(processed_images) if processed_images else np.array([])
    print(f"有效图文对数量: {len(self.reports_df)}")
```

**处理流程**:

1. 检查图像目录是否存在
2. 建立图像文件名映射
3. 根据 UID 匹配图像和报告
4. 加载和预处理图像
5. 保留有效的图文对

##### 3.3.2 结构化数据生成

```python
def _save_json_data(self):
    """生成供文档切分使用的JSON格式数据"""
    try:
        # 准备JSON数据
        json_data = []
        for idx, row in self.reports_df.iterrows():
            # 合并findings和impression作为文本内容
            text_content = ""
            if 'findings' in row and pd.notna(row['findings']):
                text_content += f"检查结果: {row['findings']}\n"
            if 'impression' in row and pd.notna(row['impression']):
                text_content += f"诊断印象: {row['impression']}\n"
            if 'MeSH' in row and pd.notna(row['MeSH']):
                text_content += f"MeSH术语: {row['MeSH']}\n"
            if 'Problems' in row and pd.notna(row['Problems']):
                text_content += f"问题描述: {row['Problems']}\n"

            # 1. 调用基类的文本清洗功能，保留医学符号
            cleaned_result = self.clean_text(text_content.strip())

            # 2. 调用数据脱敏功能
            desensitized_result = self.desensitize_text(cleaned_result['cleaned_text'])

            # 3. 创建结构化输出
            metadata = {
                'file_name': row['image'],
                'file_path': f"image_text_data/raw/chestX-rays/images/{row['image']}",
                'data_type': 'image_text',
                'image_type': 'chest_xray',
                'uid': row['uid'],
                'age': row.get('age', ''),
                'gender': row.get('gender', ''),
                'view_position': row.get('view_position', ''),
                'original_findings': row.get('findings', ''),
                'original_impression': row.get('impression', ''),
                'quality_score': cleaned_result['quality_score'],
                'medical_terms_count': cleaned_result['medical_terms_count'],
                'sensitive_info': desensitized_result['sensitive_info'],
                'has_sensitive_data': desensitized_result['has_sensitive_data']
            }

            structured_doc = self.create_structured_output(
                desensitized_result['desensitized_text'],
                metadata
            )

            json_data.append(structured_doc)

        # 保存为JSON格式
        import json
        json_path = os.path.join(self.output_dir, 'image_text_documents.json')
        with open(json_path, 'w', encoding='utf-8') as f:
            json.dump(json_data, f, ensure_ascii=False, indent=2)
        self.logger.info(f"✅ 图文JSON数据文件已保存: {json_path}")

    except Exception as e:
        self.logger.error(f"❌ 图文JSON数据文件保存失败: {e}")
```

**功能说明**:

- **文本合并**: 将检查结果、诊断印象等信息合并为完整文本
- **数据清洗**: 调用基类清洗和脱敏功能
- **元数据记录**: 记录图像类型、患者信息等元数据
- **结构化输出**: 生成标准化的 JSON 格式数据

##### 3.3.3 样本可视化

```python
def visualize_samples(self, num_samples=5):
    """可视化处理后的样本"""
    plt.figure(figsize=(15, 10))
    sample_indices = np.random.choice(len(self.reports_df), min(num_samples, len(self.reports_df)), replace=False)

    for i, idx in enumerate(sample_indices):
        plt.subplot(2, num_samples, i+1)
        # 显示原始图像
        image_name = self.reports_df.iloc[idx]['image']
        image_path = None
        for ext in ['.png', '.jpg', '.jpeg']:
            temp_path = os.path.join(self.images_dir, image_name + ext)
            if os.path.exists(temp_path):
                image_path = temp_path
                break

        if image_path:
            image = cv2.imread(image_path)
            image = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)
            plt.imshow(image)
            plt.title(f"原始图像: {image_name}")
            plt.axis('off')

        # 显示处理后的图像
        plt.subplot(2, num_samples, i+1+num_samples)
        processed_image = self.processed_images[idx].transpose(1, 2, 0)
        # 反归一化以便显示
        mean = np.array([0.485, 0.456, 0.406])
        std = np.array([0.229, 0.224, 0.225])
        processed_image = std * processed_image + mean
        processed_image = np.clip(processed_image, 0, 1)
        plt.imshow(processed_image)
        plt.title(f"处理后图像")
        plt.axis('off')

    plt.tight_layout()
    plt.savefig(os.path.join(self.output_dir, "sample_images.png"))
    print("样本图像已保存")
```

**功能说明**:

- **样本选择**: 随机选择样本进行可视化
- **对比显示**: 显示原始图像和处理后图像
- **图像保存**: 将可视化结果保存为 PNG 文件

#### 3.4 运行流程

##### 3.4.1 完整处理流程

```python
def run(self):
    """运行预处理流程"""
    try:
        self.log_processing_step("步骤1: 加载报告数据", f"从文件加载: {self.reports_file}")
        self.load_reports()
        self.logger.info(f"报告数据加载完成，共 {len(self.reports_df)} 条")

        # 在数据加载后记录开始日志
        self.log_processing_start("医疗图文数据", len(self.reports_df))

        self.log_processing_step("步骤2: 清洗报告文本", "清理文本内容和进行分词处理")
        self.clean_reports()
        self.logger.info("报告文本清洗完成")

        self.log_processing_step("步骤3: 处理图像数据", "加载和预处理图像文件")
        self.process_images()
        self.logger.info(f"图像数据处理完成，有效图文对: {len(self.reports_df)}")

        # 检查是否有有效数据
        if len(self.reports_df) == 0:
            self.logger.error("❌ 没有找到有效的图文数据。预处理终止。")
            return

        self.log_processing_step("步骤4: 保存处理后的数据", "保存图文数据到文件")
        self.save_processed_data()
        self.logger.info("数据保存完成")

        self.log_processing_step("步骤5: 生成样本可视化", "创建样本图像展示")
        self.visualize_samples()
        self.logger.info("样本可视化完成")

        self.log_processing_complete("医疗图文数据", len(self.reports_df), len(self.reports_df))
        self.logger.info("📄 图文数据预处理完成，JSON文件已保存供文档切分使用")

    except Exception as e:
        self.logger.error(f"❌ 图文数据预处理失败: {e}")
        import traceback
        traceback.print_exc()
        raise
```

**处理步骤**:

1. **加载报告数据**: 从 CSV 文件加载医疗报告
2. **清洗报告文本**: 清理文本内容和分词处理
3. **处理图像数据**: 加载和预处理图像文件
4. **保存处理数据**: 保存图文数据到文件
5. **生成可视化**: 创建样本图像展示

### 4. VoicePreprocessor - 语音数据预处理器

#### 4.1 文件概述

- **文件路径**: `processors/voice_preprocessing.py`
- **主要功能**: 专门处理语音文件转文本功能，支持多种音频格式，使用 Whisper 模型进行语音转文本
- **技术栈**: Python, Whisper, librosa, soundfile, numpy

#### 4.2 核心组件

##### 4.2.1 初始化配置

```python
def __init__(self,
             raw_data_dir: str,
             output_dir: str,
             whisper_model_path: str = None,
             sample_rate: int = None):
    """
    初始化语音预处理器

    Args:
        raw_data_dir: 原始语音数据目录
        output_dir: 输出目录
        whisper_model_path: Whisper模型路径（可选，将从配置中获取）
        sample_rate: 音频采样率（可选，将从配置中获取）
    """
    # 调用父类初始化
    super().__init__(raw_data_dir, output_dir, "VoicePreprocessor")

    # 从配置中获取模型信息
    try:
        import sys
        from pathlib import Path
        current_dir = Path(__file__).parent
        sys.path.append(str(current_dir.parent.parent.parent / "llm_models"))

        from model_config_reader import get_model_config_reader

        reader = get_model_config_reader()
        voice_model_info = reader.get_voice_to_text_model()

        # 设置模型参数
        self.sample_rate = sample_rate or voice_model_info.get('sample_rate', 16000)
        self.whisper_model_name = voice_model_info.get('model_name', 'whisper-tiny')
        self.whisper_model_path = voice_model_info.get('model_path', '')

    except Exception as e:
        self.logger.warning(f"无法从配置加载模型信息: {e}")
        # 使用默认值
        self.sample_rate = sample_rate or 16000
        self.whisper_model_name = "tiny"
        self.whisper_model_path = whisper_model_path or ""

    # 支持的音频格式
    self.supported_formats = {
        '.wav': 'WAV音频文件',
        '.mp3': 'MP3音频文件',
        '.m4a': 'M4A音频文件',
        '.flac': 'FLAC音频文件',
        '.aac': 'AAC音频文件',
        '.ogg': 'OGG音频文件'
    }

    # 初始化Whisper模型
    self.whisper_model = None
    self._load_whisper_model()
```

**配置说明**:

- **raw_data_dir**: 原始语音数据目录
- **output_dir**: 输出目录
- **whisper_model_name**: Whisper 模型名称
- **sample_rate**: 音频采样率
- **supported_formats**: 支持的音频格式

##### 4.2.2 Whisper 模型加载

```python
def _load_whisper_model(self):
    """加载Whisper模型"""
    if not VOICE_LIBRARIES_AVAILABLE:
        self.logger.warning("语音处理库不可用，跳过Whisper模型加载")
        return

    try:
        self.logger.info(f"🔧 加载Whisper模型: {self.whisper_model_name}")
        self.whisper_model = whisper.load_model(self.whisper_model_name)
        self.logger.info("✅ Whisper模型加载成功")
    except Exception as e:
        self.logger.error(f"❌ Whisper模型加载失败: {e}")
        self.whisper_model = None
```

**功能说明**:

- 检查语音处理库是否可用
- 加载指定的 Whisper 模型
- 处理模型加载失败的情况

##### 4.2.3 语音类型检测

```python
def _detect_voice_type_from_filename(self, filename: str) -> str:
    """根据文件名判断语音类型"""
    filename_lower = filename.lower()

    # 医疗对话语音
    if any(keyword in filename_lower for keyword in ['dialogue', '对话', 'chat', 'conversation', 'consultation']):
        return 'medical_dialogue'

    # 医疗讲座/培训
    elif any(keyword in filename_lower for keyword in ['lecture', '讲座', 'training', '培训', 'course']):
        return 'medical_lecture'

    # 医疗咨询
    elif any(keyword in filename_lower for keyword in ['consultation', '咨询', 'advice', '建议']):
        return 'medical_consultation'

    # 病历录音
    elif any(keyword in filename_lower for keyword in ['record', '病历', 'case', '病例']):
        return 'medical_record'

    # 健康宣教
    elif any(keyword in filename_lower for keyword in ['education', '宣教', 'health', '健康']):
        return 'health_education'

    # 默认为通用语音
    else:
        return 'general_voice'
```

**语音类型**:

- **medical_dialogue**: 医疗对话语音
- **medical_lecture**: 医疗讲座/培训
- **medical_consultation**: 医疗咨询
- **medical_record**: 病历录音
- **health_education**: 健康宣教
- **general_voice**: 通用语音

#### 4.3 核心处理方法

##### 4.3.1 音频预处理

```python
def _preprocess_audio(self, audio_path: Path) -> Tuple[np.ndarray, int]:
    """预处理音频文件"""
    try:
        # 使用librosa加载音频，添加警告过滤
        with warnings.catch_warnings():
            warnings.simplefilter("ignore")
            audio, sr = librosa.load(str(audio_path), sr=self.sample_rate)

        # 音频预处理
        # 1. 去除静音段
        audio, _ = librosa.effects.trim(audio, top_db=20)

        # 2. 归一化
        audio = librosa.util.normalize(audio)

        return audio, sr
    except Exception as e:
        self.logger.error(f"音频预处理失败 {audio_path}: {e}")
        raise
```

**处理流程**:

1. 使用 librosa 加载音频文件
2. 去除静音段
3. 音频归一化处理

##### 4.3.2 语音转文本

```python
def _transcribe_audio(self, audio_path: Path) -> Dict[str, Any]:
    """使用Whisper转录音频"""
    if self.whisper_model is None:
        raise RuntimeError("Whisper模型未加载")

    try:
        # 预处理音频
        audio, sr = self._preprocess_audio(audio_path)

        # 使用Whisper转录音频，添加警告过滤
        with warnings.catch_warnings():
            warnings.simplefilter("ignore")
            result = self.whisper_model.transcribe(
                audio,
                language="zh",  # 指定中文
                task="transcribe",
                verbose=False
            )

        # 提取转录结果
        transcription = {
            'text': result['text'].strip(),
            'language': result.get('language', 'zh'),
            'duration': len(audio) / sr,
            'segments': []
        }

        # 处理分段信息
        if 'segments' in result:
            for segment in result['segments']:
                transcription['segments'].append({
                    'start': segment['start'],
                    'end': segment['end'],
                    'text': segment['text'].strip(),
                    'confidence': segment.get('no_speech_prob', 0.0)
                })

        return transcription

    except Exception as e:
        self.logger.error(f"音频转录失败 {audio_path}: {e}")
        raise
```

**处理流程**:

1. 检查 Whisper 模型是否加载
2. 预处理音频文件
3. 使用 Whisper 进行语音转文本
4. 提取转录结果和分段信息

##### 4.3.3 转录文本清洗

```python
def _clean_transcription_text(self, text: str) -> str:
    """清洗转录文本"""
    if not text:
        return ""

    # 使用基类清洗方法进行清洗
    cleaned_result = self.clean_text(text)
    cleaned_text = cleaned_result.get('cleaned_text', text)

    # 语音特有的清洗规则
    # 1. 移除重复的标点符号
    import re
    cleaned_text = re.sub(r'([。！？，；：])\1+', r'\1', cleaned_text)

    # 2. 修复常见的语音识别错误
    corrections = {
        '嗯': '嗯',
        '啊': '啊',
        '呃': '呃',
        '这个': '这个',
        '那个': '那个',
        '就是': '就是',
        '然后': '然后',
        '所以': '所以',
        '因为': '因为',
        '但是': '但是'
    }

    for wrong, correct in corrections.items():
        cleaned_text = cleaned_text.replace(wrong, correct)

    return cleaned_text
```

**清洗规则**:

- 使用基类清洗方法
- 移除重复标点符号
- 修复常见语音识别错误

##### 4.3.4 单文件处理

```python
def process_single_voice_file(self, file_path: Path) -> Dict[str, Any]:
    """处理单个语音文件"""
    try:
        self.logger.info(f"🎤 处理语音文件: {file_path.name}")

        # 检测语音类型
        voice_type = self._detect_voice_type_from_filename(file_path.name)

        # 转录音频
        transcription = self._transcribe_audio(file_path)

        # 1. 调用基类的文本清洗功能，保留医学符号
        cleaned_result = self.clean_text(transcription['text'])

        # 2. 调用数据脱敏功能
        desensitized_result = self.desensitize_text(cleaned_result['cleaned_text'])

        # 3. 创建结构化输出
        metadata = {
            'file_name': file_path.name,
            'file_path': str(file_path),
            'data_type': 'voice',
            'voice_type': voice_type,
            'duration': transcription['duration'],
            'language': transcription['language'],
            'file_size': file_path.stat().st_size,
            'quality_score': cleaned_result['quality_score'],
            'medical_terms_count': cleaned_result['medical_terms_count'],
            'sensitive_info': desensitized_result['sensitive_info'],
            'has_sensitive_data': desensitized_result['has_sensitive_data']
        }

        structured_doc = self.create_structured_output(
            desensitized_result['desensitized_text'],
            metadata
        )

        # 构建结果（保持向后兼容）
        result = {
            'file_name': file_path.name,
            'file_path': str(file_path),
            'voice_type': voice_type,
            'duration': transcription['duration'],
            'language': transcription['language'],
            'original_text': transcription['text'],
            'cleaned_text': desensitized_result['desensitized_text'],
            'segments': transcription['segments'],
            'processed_time': datetime.now().isoformat(),
            'file_size': file_path.stat().st_size,
            'structured_doc': structured_doc  # 添加结构化文档
        }

        # 更新统计
        self.total_duration += transcription['duration']
        self.processed_files.append(result)

        self.logger.info(f"✅ 语音文件处理完成: {file_path.name} (时长: {transcription['duration']:.2f}s)")
        return result

    except Exception as e:
        error_info = {
            'file_name': file_path.name,
            'file_path': str(file_path),
            'error': str(e),
            'processed_time': datetime.now().isoformat()
        }
        self.failed_files.append(error_info)
        self.logger.error(f"❌ 语音文件处理失败: {file_path.name} - {e}")
        return None
```

**处理流程**:

1. 检测语音类型
2. 转录音频文件
3. 清洗和脱敏文本
4. 创建结构化输出
5. 更新处理统计

#### 4.4 批量处理功能

##### 4.4.1 文件扫描

```python
def scan_voice_files(self) -> List[Path]:
    """扫描语音文件"""
    voice_files = []

    # 扫描原始数据目录
    if self.raw_data_dir.exists():
        for file_path in self.raw_data_dir.rglob('*'):
            if file_path.is_file() and self._is_audio_file(file_path):
                voice_files.append(file_path)

    # 扫描专门的语音数据目录
    voice_data_dir = self.raw_data_dir.parent / 'voice_data' / 'raw'
    if voice_data_dir.exists():
        for file_path in voice_data_dir.rglob('*'):
            if file_path.is_file() and self._is_audio_file(file_path):
                voice_files.append(file_path)

    self.logger.info(f"🔍 发现 {len(voice_files)} 个语音文件")
    return voice_files
```

##### 4.4.2 批量处理

```python
def run(self, sample_size: Optional[int] = None, process_all_files: bool = True):
    """运行语音处理"""
    self.logger.info("🚀 开始语音处理")

    # 检查语音处理库
    if not VOICE_LIBRARIES_AVAILABLE:
        self.logger.error("❌ 语音处理库不可用，无法处理语音文件")
        return

    # 检查Whisper模型
    if self.whisper_model is None:
        self.logger.error("❌ Whisper模型未加载，无法处理语音文件")
        return

    # 扫描语音文件
    voice_files = self.scan_voice_files()

    if not voice_files:
        self.logger.warning("⚠️ 未发现语音文件")
        return

    # 限制处理数量
    if sample_size and sample_size > 0:
        voice_files = voice_files[:sample_size]
        self.logger.info(f"📊 限制处理数量: {len(voice_files)} 个文件")

    # 处理语音文件
    results = []
    for i, file_path in enumerate(voice_files, 1):
        self.logger.info(f"📁 处理进度: {i}/{len(voice_files)} - {file_path.name}")

        result = self.process_single_voice_file(file_path)
        if result:
            results.append(result)

    # 保存结果
    if results:
        self.save_results(results)
        self.logger.info(f"🎉 语音处理完成! 成功处理 {len(results)} 个文件")
        self.logger.info("📄 语音数据预处理完成，JSON文件已保存供文档切分使用")
    else:
        self.logger.warning("⚠️ 没有成功处理任何文件")

    # 输出失败信息
    if self.failed_files:
        self.logger.warning(f"⚠️ {len(self.failed_files)} 个文件处理失败")
        for failed in self.failed_files:
            self.logger.warning(f"  - {failed['file_name']}: {failed['error']}")
```

**功能说明**:

- **库检查**: 检查语音处理库和 Whisper 模型是否可用
- **文件扫描**: 扫描语音文件目录
- **批量处理**: 逐个处理语音文件
- **结果保存**: 保存处理结果和统计信息
- **错误处理**: 记录处理失败的文件

## 5. 数据管道 (DataPipeline)

### 5.1 文件概述

- **文件路径**: `processors/data_pipeline.py`
- **主要功能**: 统一调度和管理所有数据处理器，提供完整的数据处理流程
- **技术栈**: Python, asyncio, concurrent.futures

### 5.2 核心组件

#### 5.2.1 初始化配置

```python
def __init__(self, raw_data_dir: str, output_dir: str):
    """
    初始化数据管道

    Args:
        raw_data_dir: 原始数据目录
        output_dir: 输出目录
    """
    self.raw_data_dir = Path(raw_data_dir)
    self.output_dir = Path(output_dir)

    # 创建输出目录
    self.output_dir.mkdir(parents=True, exist_ok=True)

    # 初始化处理器
    self.processors = {}
    self._init_processors()

    # 处理统计
    self.processing_stats = {
        'total_files': 0,
        'processed_files': 0,
        'failed_files': 0,
        'processing_time': 0
    }
```

#### 5.2.2 处理器初始化

```python
def _init_processors(self):
    """初始化所有数据处理器"""
    try:
        # 文本处理器
        self.processors['text'] = TextPreprocessor(
            self.raw_data_dir,
            self.output_dir / 'text_processed'
        )

        # 图文处理器
        self.processors['image_text'] = ImageTextPreprocessor(
            self.raw_data_dir / 'image_text_data'
        )

        # 语音处理器
        self.processors['voice'] = VoicePreprocessor(
            self.raw_data_dir,
            self.output_dir / 'voice_processed'
        )

        self.logger.info("✅ 所有数据处理器初始化完成")

    except Exception as e:
        self.logger.error(f"❌ 处理器初始化失败: {e}")
        raise
```

### 5.3 核心处理方法

#### 5.3.1 并行处理

```python
def process_all_data(self, sample_size: Optional[int] = None,
                    parallel: bool = True, max_workers: int = 4):
    """
    处理所有类型的数据

    Args:
        sample_size: 每种数据类型的样本数量限制
        parallel: 是否并行处理
        max_workers: 最大并行工作线程数
    """
    start_time = time.time()

    if parallel:
        # 并行处理
        with ThreadPoolExecutor(max_workers=max_workers) as executor:
            futures = {}

            # 提交处理任务
            for data_type, processor in self.processors.items():
                future = executor.submit(
                    self._process_data_type,
                    data_type, processor, sample_size
                )
                futures[future] = data_type

            # 收集结果
            for future in as_completed(futures):
                data_type = futures[future]
                try:
                    result = future.result()
                    self.logger.info(f"✅ {data_type} 数据处理完成")
                except Exception as e:
                    self.logger.error(f"❌ {data_type} 数据处理失败: {e}")
    else:
        # 串行处理
        for data_type, processor in self.processors.items():
            try:
                self._process_data_type(data_type, processor, sample_size)
                self.logger.info(f"✅ {data_type} 数据处理完成")
            except Exception as e:
                self.logger.error(f"❌ {data_type} 数据处理失败: {e}")

    # 计算处理时间
    processing_time = time.time() - start_time
    self.processing_stats['processing_time'] = processing_time

    # 生成处理报告
    self._generate_processing_report()
```

#### 5.3.2 数据类型处理

```python
def _process_data_type(self, data_type: str, processor, sample_size: Optional[int]):
    """处理特定类型的数据"""
    try:
        self.logger.info(f"🚀 开始处理 {data_type} 数据")

        # 根据数据类型调用相应的处理方法
        if data_type == 'text':
            processor.run(sample_size=sample_size)
        elif data_type == 'image_text':
            processor.run()
        elif data_type == 'voice':
            processor.run(sample_size=sample_size)

        self.logger.info(f"✅ {data_type} 数据处理完成")

    except Exception as e:
        self.logger.error(f"❌ {data_type} 数据处理失败: {e}")
        raise
```

#### 5.3.3 处理报告生成

```python
def _generate_processing_report(self):
    """生成处理报告"""
    try:
        report = {
            'processing_time': self.processing_stats['processing_time'],
            'timestamp': datetime.now().isoformat(),
            'data_types': {}
        }

        # 收集各处理器的统计信息
        for data_type, processor in self.processors.items():
            if hasattr(processor, 'processed_files'):
                report['data_types'][data_type] = {
                    'processed_files': len(processor.processed_files),
                    'failed_files': len(processor.failed_files),
                    'total_duration': getattr(processor, 'total_duration', 0)
                }

        # 保存报告
        report_path = self.output_dir / 'processing_report.json'
        with open(report_path, 'w', encoding='utf-8') as f:
            json.dump(report, f, ensure_ascii=False, indent=2)

        self.logger.info(f"📊 处理报告已保存: {report_path}")

    except Exception as e:
        self.logger.error(f"❌ 处理报告生成失败: {e}")
```

### 5.4 使用示例

#### 5.4.1 基本使用

```python
# 创建数据管道
pipeline = DataPipeline(
    raw_data_dir="datas/medical_knowledge",
    output_dir="datas/medical_knowledge/processed"
)

# 处理所有数据
pipeline.process_all_data(
    sample_size=100,  # 每种数据类型限制100个样本
    parallel=True,    # 并行处理
    max_workers=4     # 最大4个并行线程
)
```

#### 5.4.2 单独处理特定类型

```python
# 只处理文本数据
pipeline.processors['text'].run(sample_size=50)

# 只处理语音数据
pipeline.processors['voice'].run(sample_size=20)
```

---

## 6. 总结

### 6.1 架构特点

智诊通向量化服务的数据处理层采用**分层架构设计**，具有以下特点：

1. **统一基类**: `BaseProcessor` 提供通用的数据处理功能
2. **专业化处理**: 针对不同数据类型提供专门的处理器
3. **标准化输出**: 所有处理器都生成统一的结构化数据格式
4. **并行处理**: 支持多线程并行处理，提高处理效率
5. **错误处理**: 完善的错误处理和日志记录机制

### 6.2 核心功能

#### 6.2.1 数据清洗

- **乱码清理**: 自动识别和清理乱码字符
- **广告清理**: 识别和删除广告内容
- **格式统一**: 统一文本格式和编码

#### 6.2.2 数据脱敏

- **个人信息保护**: 自动识别和脱敏身份证、手机号等敏感信息
- **医疗数据保护**: 保护患者隐私和医疗记录安全
- **可配置脱敏**: 支持自定义脱敏规则

#### 6.2.3 医学符号保留

- **医学术语**: 保留专业医学术语和符号
- **检验指标**: 保留检验结果和医学单位
- **诊断符号**: 保留诊断相关的特殊符号

### 6.3 支持的数据类型

| 数据类型 | 支持格式                   | 处理器                | 主要功能                       |
| -------- | -------------------------- | --------------------- | ------------------------------ |
| 文本数据 | TXT, PDF, CSV, JSON, Excel | TextPreprocessor      | 文本提取、清洗、结构化         |
| 图文数据 | 图像 + CSV 报告            | ImageTextPreprocessor | 图像预处理、文本提取、图文匹配 |
| 语音数据 | WAV, MP3, M4A, FLAC 等     | VoicePreprocessor     | 语音转文本、音频预处理         |

### 6.4 输出格式

所有处理器都生成统一的结构化输出格式：

```json
{
  "id": "唯一文档ID",
  "content": "清洗后的文本内容",
  "metadata": {
    "file_name": "原始文件名",
    "file_path": "文件路径",
    "data_type": "数据类型",
    "processing_time": "处理时间",
    "quality_score": "质量评分",
    "medical_terms_count": "医学术语数量",
    "sensitive_info": "敏感信息列表",
    "has_sensitive_data": "是否包含敏感数据"
  }
}
```

### 6.5 性能优化

1. **并行处理**: 支持多线程并行处理不同类型数据
2. **内存管理**: 优化内存使用，支持大文件处理
3. **进度监控**: 实时显示处理进度和状态
4. **错误恢复**: 支持部分失败后的错误恢复

### 6.6 扩展性

数据处理层设计具有良好的扩展性：

1. **新数据类型**: 可以轻松添加新的数据类型处理器
2. **自定义清洗规则**: 支持自定义数据清洗和脱敏规则
3. **插件化架构**: 支持插件化的处理组件
4. **配置驱动**: 通过配置文件控制处理参数

---

## 7. 使用指南

### 7.1 环境准备

```bash
# 安装依赖
pip install -r requirements.txt

# 安装语音处理依赖（可选）
pip install whisper librosa soundfile

# 安装图像处理依赖（可选）
pip install opencv-python pillow torch torchvision
```

### 7.2 快速开始

```python
from processors.data_pipeline import DataPipeline

# 1. 创建数据管道
pipeline = DataPipeline(
    raw_data_dir="your_raw_data_path",
    output_dir="your_output_path"
)

# 2. 处理所有数据
pipeline.process_all_data(
    sample_size=100,
    parallel=True
)

# 3. 查看处理结果
print("处理完成！结果保存在:", pipeline.output_dir)
```

### 7.3 配置说明

数据处理层的配置主要通过以下方式：

1. **环境变量**: 设置数据路径和模型路径
2. **配置文件**: 通过 `model_config_reader` 读取模型配置
3. **参数传递**: 在初始化时传递处理参数

### 7.4 常见问题

#### 7.4.1 内存不足

- 减少 `sample_size` 参数
- 使用串行处理模式
- 增加系统内存

#### 7.4.2 处理速度慢

- 启用并行处理模式
- 增加 `max_workers` 参数
- 使用更快的存储设备

#### 7.4.3 模型加载失败

- 检查模型文件路径
- 确认模型文件完整性
- 检查依赖库安装

---

**文档版本**: v1.0  
**最后更新**: 2024 年 12 月  
**维护团队**: 智诊通开发团队
